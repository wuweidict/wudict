# Phase plan & record

Each phase ends with: working code + tests, PHASES record entry (below), DECISIONS updated. Keep record entries ≤5 lines.

## Plan

- **P0 — Research & scaffolding.** Survey references, audit draego FTS, settle architecture, write CLAUDE.md/SPEC/FORMATS/DECISIONS/PHASES. Gate: D1–D2 taken; D3–D7, D9 answered.
- **P1 — Core + MDX direct backend.** New Go module; `Dictionary`/`Lookuper`/`Reader` interfaces + capability tiers; inline `gomdict`; MDX/MDD direct lookup + resource serving; CLI `lookup`; golden tests on a small sample dict.
- **P2 — Ingest tier + query engine.** Canonical text.db schema; MDX ingester; ported draego query modes with all SPEC §FTS-audit fixes; concurrent all-dicts fan-out across mixed backends; driver benchmark settles D4; hostile-input FTS tests (`-`, `*`, `"`, `NEAR`, emoji).
- **P3 — StarDict + Slob.** Each: direct `Lookuper` (idx binary search / in-memory refs) + `Reader` for ingest + resource resolver (res dir/zip, slob bins). Sample-file tests.
- **P4 — DSL.** Lexer + →HTML transform (port rules from pyglossary dsl plugin), encodings (UTF-8/16/32+BOM), `.dsl.dz`, `.files.zip` resolver; transparent auto-ingest on first open. Heaviest parsing work — isolated phase.
- **P5 — HTTP server & API + full ingest.** Config layering from mdict-go-web; endpoints per SPEC §6; per-dict "Enable fuzzy & full-text search" flow with SSE progress; `ingest=full` media.db packing + uuid pairing + resolution order.
- **P6 — UI.** draego chrome (sticky bar, accordion, vendored mark.js) + Shadow-DOM article sandboxing; per-dict CSS into shadow roots; φ-scale spacing; mobile; dark mode (mdict-go-web/notes/dark-mode.md).
- **P7 — Polish & release.** Cross-platform builds, Makefile, README.md, stale-source handling UX, perf pass, error-path hardening.
- **P8+ — user-directed batches** (no pre-written plan; each lands as a record entry below): P8/P8.1 performance & progressive UX, P9 audit fixes & URL/MIME correctness, P10 native-ingested overhaul (+BGL), P11 preview-cost & in-process Speex. Standing backlog: `code-review.md`, prioritized by D15.

## Record

### P0 — 2026-07-24 — COMPLETE (revised same day)
Surveyed mdict-go-web (gomdict reusable as-is), draego (FTS5 logic reusable after 8 audited fixes — SPEC §FTS-audit), pyglossary readers (pointer map in FORMATS.md). Initial hybrid-ingest proposal revised per user: **dual-backend** — direct readers default, opt-in `ingest=off|text|full`, media in separate `<slug>.media.db` (D1/D2 ACCEPTED, D9 naming proposed). Docs updated to v0.2. Same day: D4 (mattn/go-sqlite3 + sqlite_fts5), D5 ("Enable fuzzy & full-text search"), D7 (cache dir only, UI must expose DB paths for sharing), D9 (.text.db/.media.db) ACCEPTED. Later same day: D3 (persistent server) and D6 (`github.com/wuweidict/wudict`) ACCEPTED; D10 (Makefile = developer UI) added.

### P1 — 2026-07-24 — COMPLETE
Created `wudict/` module: `internal/dict` (Dictionary/Caps/Reader interfaces + extension registry with Discover), inlined `internal/gomdict` (go-logging dep replaced by env-gated stderr shim, debug-only mdict_extend.go dropped), `internal/format/mdx` direct backend (map-indexed exact/fold lookup — was O(n) scans in mdict-go-web — @@@LINK following, StyleSheet substitution, direct .mdd resource streaming with traversal guard, companion NAME.N.mdd discovery), CLI `list/info/lookup/prefix/keys/res`, Makefile per D10 (`make check` = tidy+vet+test). Verified against real dicts (Espasa-Calpe 38.5k entries + 106MB MDD): exact, accent-fold (corazon→corazón), prefix, keys paging, ogg resource extraction all work. Unit + env-gated integration tests pass.

### P2 — 2026-07-24 — COMPLETE
Ingest tier + query engine live. `internal/store`: canonical text.db (SPEC §2, contentless FTS5, unicode61 remove_diacritics 2), atomic tmp-file ingest with alias/@@@LINK resolution, StripHTML (script/style-aware), Slug/DefaultDBDir per D7/D9; all 8 §FTS-audit fixes implemented + tested (hostile MATCH inputs, LIKE escaping, clamped limits). `internal/search`: bounded-concurrency all-dicts fan-out with capability skipping, order-preserving. mdx gained an ingest Reader (`LinkTo` redirects); CLI gained `ingest/fuzzy/fts/searchall`; Makefile gained `ingest` target. Real-world check: Espasa-Calpe 38.5k entries → 46MB text.db in 6.9s (mattn driver perf fine — D4 confirmed); accent-insensitive fuzzy (`corazon`→corazón/corazonada) and BM25 FTS (`organo muscular`→músculo, corazón) verified.

### P3 — 2026-07-24 — COMPLETE
StarDict + Slob landed, each with direct `Lookuper` + ingest `Reader` + resource resolver. `internal/format/stardict`: ifo/idx(.gz)/syn parsing (32/64-bit offsets), plain `.dict` ReadAt + random-access **dictzip** reader (RA chunk table, LRU), sametypesequence & per-part typed records, minimal XDXF→HTML, res/ dir + res.zip resolver. `internal/format/slob`: full container parse (header/editable tags/refs/store) ported from pyglossary layout, zlib/bz2/lzma2 bins (ulikunitz/xz), in-memory refs + exact/fold maps (no ICU needed), bin-LRU blob fetch, bin-ordered ingest that folds multi-key blobs into headword+aliases and leaves resource blobs lazy. Shared `dict.Fold`. Synthetic-fixture unit tests (incl. hand-built .slob and dictzip) + env-gated integration tests. Cross-check: Espasa slob ingest → 38,521 entries, byte-identical count to the MDX edition; Stanford stardict ingest 5.1s, FTS live.

### P4 — 2026-07-24 — COMPLETE
Lingvo DSL landed as `internal/format/dsl`: byte-lexer transformer ported from pyglossary (tags [b/i/u/'/sup/sub/c/m/p/*/ex/t/s/ref/url], {{comments}}, ~ substitution, <<links>>, \-escapes), title transformer (Full/Alt paren variants, {curly} display parts), block reader (headword/indent structure, "@" sub-entries linked from parent), encoding sniff (BOM: UTF-8/16LE/16BE/32 + BOM-less fallbacks), `.dsl.dz` via gzip, resources from `.files.zip`/loose files. Direct backend = transparent auto-ingest into `<slug>-<hash8>.text.db` (source-hash-keyed cache; WUDICT_DB_DIR override). Robustness beyond pyglossary: malformed tags (literal `([ ])` in the corchete entry) degrade to text instead of killing the entry. Two real bugs caught by fixture: byte→rune mojibake (corazón→corazÃ³n) and store.Exact lacking accent-fold (fixed with exact-phrase FTS + Fold filter, parity with direct backends). Espasa DSL: 38,522 entries in ~11s incl. FTS; lookup/fuzzy/fts/res(325MB zip) verified.

### P5 — 2026-07-24 — COMPLETE
HTTP server + full ingest live. `internal/config`: flag>env>toml>default layering (mdict-go-web style, minimal flat-TOML parser, ~-expansion, search order explicit/exe-dir/~/.wudict/cwd). `internal/server`: lazy-open registry (id = path-hash12, Rescan keeps open handles), `upgraded` wrapper (text.db queries + media.db→source resource order per D2, race-safe swap after ingest), endpoints /api/dicts (exposes DBPath per D7), /api/search (mixed-backend fan-out), /api/ingest (SSE progress/done/error, per-dict mutex, full=media pack), /api/rescan, /res/{id}/{name} (mime + cache headers), embedded provisional index.html (bword:// nav, audio click, /res rewrite, upgrade-link with EventSource progress — real UI comes in P6). `media.db` (SPEC §3) via dict.ResourceLister implemented by all 4 formats (slob lists via bin-header-only ctype reads). store.CacheBase unifies slug-hash8 cache naming (dsl refactored onto it); WUDICT_DB_DIR override; CLI `ingest -full` + `serve`; Makefile `serve`. Live-verified: wordref MDX SSE upgrade (fuzzy corazon→corazón/corazonada), 19MB MDD→30MB media.db uuid-paired, /res serves audio/mpeg from media.db.

### P6 — 2026-07-24 — COMPLETE
Real UI shipped (embedded, replaces provisional page). Design-token layer (φ spacing scale --sp-1..5, semantic color vars) with three themes: auto (prefers-color-scheme) / light / dark, persisted, cycled via ◐ button. **Shadow-DOM sandbox per article** — dictionary HTML/CSS renders inside a shadow root with :host{all:initial} base, so dict styles can never touch chrome and vice-versa (fixed: <dd> can't attachShadow → inner div host). Dark mode inverts articles Dark-Reader-style per mdict-go-web/notes/dark-mode.md (invert+hue-rotate, media flipped back inside the root); verified visually via headless Chrome screenshots — dict hue-coding survives. Chrome: sticky auto-hide bar, accordion cards, live search (300ms debounce, seq-guarded), "/" shortcut, loading/empty/error states, vendored mark.js served from /assets (highlights work inside shadow roots), bword://+bare-word link nav, audio click-through (incl. DSL <object>), dictionaries side panel (caps tags, "Enable fuzzy & full-text search"/"pack media" actions with EventSource progress, click-to-copy db path per D7, rescan), shareable ?q=&mode=&dict=&theme= URLs, mobile breakpoint. Server: /assets/mark.min.js embedded+cached.

### P6.5 — 2026-07-24 — COMPLETE (uncommitted, user-requested pre-P7 fixes)
(1) Result-summary "enable fuzzy & full-text search" link had no click handler (only the panel was wired in P6) — fixed with preventDefault against accordion toggle, busy-guard, live SSE counter; verified by CDP-driven real click (Stanford ingested, search re-ran with 20 hits). (2) mdict-go-web-grade `--help`: full serve flags with env/toml/default, config search order, priority chain, example config.toml, examples; `-v/--version` (Makefile stamps via -X main.version); bare `wudict` and bare-flags invocations default to `serve`. (3) First-run UX: startup message states missing/empty dict folder instead of lying "serving X"; commented-defaults config.toml auto-generated (exe dir, fallback ~/.wudict — read-only-safe); when the registry is empty "/" serves an embedded setup page — live-validated folder input (`/api/setup`, shows dictionary count), save switches the registry live (no restart) and persists DICT_DIR via config.SaveKey (uncomment-in-place, preserves comments). Tests: SaveKey round-trip, full setup-flow httptest; curl smoke of the entire first-run path (51 dicts found on save).

### P6.6 — 2026-07-25 — COMPLETE (uncommitted, user TODO batch)
(1) `--verbose` (+VERBOSE/WUDICT_VERBOSE env, toml key) via new `internal/logx`: request timing, dictionary opens, ingests, spx transcodes; `-v` stays version (mdict parity, noted in help). (2) Dropdown + panel now sorted case-insensitively by display name (localeCompare sensitivity:base) — registry sorts by path, which looked random once titles were shown. (3) Post-setup blank-dropdown panic fixed twice over: /api/dicts now opens dictionaries in parallel (8 workers — was serial, seconds for 50 dicts) and the UI shows a "Loading dictionaries…" spinner while the list loads. (4) .spx audio: external speexdec (go-speex rejected — cgo binding to system libspeex = distribution pain; no pure-Go decoder exists); server transcodes at /res time with WAV cache under db-dir/spxcache, SPEEXDEC config (PATH-discovered default), graceful raw fallback; verified on MW11sound (RIFF WAV out, 546ms→0.9ms cached). (5) Shadow-root JS verdict: innerHTML never executes <script> and dict JS can't see shadow content — unfixable cleanly; hybrid renderer instead: script-free articles keep the light shadow sandbox, script-bearing ones auto-detect into a **sandboxed iframe** (srcdoc + /assets/frame.js bridge: ResizeObserver height, bword:// lookup + dblclick postMessage, theme sync, self-inverting dark mode excluded from outer invert). CDP-verified: Stanford Bayesian-Epistemology renders its own CSS with 572 MathJax formulas typeset in-frame, height bridge 48.5k px. Known cost: wide fixed-width dict layouts scroll horizontally inside the frame; N script-heavy results = N iframes (deferred-hydration a future option).

### P6.7 — 2026-07-25 — COMPLETE (uncommitted, user TODO batch 2)
(1) Article v-scrollbars: `.article{overflow-x:auto}` implicitly computes overflow-y:auto → every shadow article scrolled; fixed with explicit overflow-y:hidden; iframes: robust height measurement (max of scrollHeight/offsetHeight+16) + internal html{overflow-y:hidden}. CDP-verified: 0 articles with internal v-scroll. (2) Layout review: column widened 680→800px (fits Stanford-class ~750px fixed layouts without h-scroll), bar matches; persisted ⇔ wide-toggle (min(1280px,96vw)) — judged a legitimate reader affordance atop a fluid default, not an anti-pattern. (3) Ingest-everything + fuzzy-only: new `store.Level` (text|headwords) — headwords-only DB skips article-text indexing (much smaller), Caps advertises fuzzy without FTS, FullText returns ErrUnsupported, auto-upgrades (delete+rebuild) when full text requested later; CLI `ingest <folder>` batch-ingests all discovered dicts (skip already-done) and `-fuzzy-only`; API `level=headwords`; UI panel per-dict acts (Enable fuzzy&FTS / Fuzzy only / Full+pack media, plus "Add full-text search" upgrade for headwords-only dicts) and "⚡ enable all" sequential batch button. Clarified: fuzzy always queried headwords only — the new level makes the *index* smaller, not the query different.

### P8 — Performance & progressive UX — 2026-07-25 — COMPLETE (uncommitted, user-directed)
Audited every lookup path; root causes of "prefix/exact takes seconds": (a) `/api/dicts` force-built the heavy in-memory index for **all** dicts on page load; (b) `openUpgradedOrDirect` **double-opened** ingested dicts (full direct decompress + fold-maps *plus* the SQLite open) just to keep `src` for resource fallback. Fixes (A–E, all A–E done): **P-A** cheap header-only `dict.Probe` (mdx via `gomdict.EntryCount()`; stardict via .ifo) + `dict.HasProber`; `upgraded.src` now **lazy** (opened only on a resource-fallback miss) and resources route through the embedded Store's auto-attached media.db first — kills the double-open and the RAM held for fold-maps; `/api/dicts` resolves rows from Probe+`store.ReadMeta` without a full open; `Registry.Warm()` background-opens all dicts (bounded 4) off the request path. **P-B** `/api/search` streams **NDJSON** (`begin`/`hit`/`end`) via new `search.Stream`; each `hit` carries its preference slot index so the client renders each accordion the instant it completes and auto-opens the highest-preference non-empty dict. **P-C** panel gains drag-gripper + ▲▼⏫⏬ reorder and an enable/disable switch, persisted (`wudict_order`/`wudict_disabled`); "All" queries only enabled dicts in preference order (dropdown stays A→Z for name lookup). **P-D** fewer results/dict for All (5) vs single (25) + per-dict "more…" re-query. **P-E** `store.Prefix` folded-FTS fallback (accent-insensitive prefix parity) + `_busy_timeout` on RO dsn. Concurrency bug caught under purego: Probe's full-Open fallback for non-probeable formats (DSL auto-ingest) raced `Warm` and clobbered the shared `.ingest` temp → guarded both call sites behind `HasProber` + made ingest temp files unique (`tempDBName`). make check green on both drivers incl. real fixtures; race detector clean; JS node-checked.

### P8.1 — Auto-fuzzy on first search — 2026-07-25 — COMPLETE (uncommitted, user-directed)
Fuzzy indexing is now transparent (D13). New `AUTO_INDEX` (default on; the value was spelled `fuzzy` until 2026-07-28); `Server.AutoIndex`. `entry.maybeAutoIndex()` (a `sync.Once`, guarded by `Caps.Fuzzy`) fires from `handleSearch` on the first search of a direct-only dict and builds a headwords-level index in the background — no first-run storm (`Warm` still only opens), failures swallowed. UI: panel drops "Fuzzy only", "Enable fuzzy & full-text search"→"full-text search", "Full + pack media"→"FTS + pack media", "enable all" tooltip now full-text; a fuzzy hit on a not-yet-built dict renders "preparing fuzzy index…", full-text keeps its explicit link. README + config template updated. Test `TestAutoIndexOnFirstSearch` (synthetic `.fake` direct format → prefix search → poll until accent-folded fuzzy resolves). Both drivers green, race clean.

### P7 (trimmed) — 2026-07-25 — COMPLETE (uncommitted)
Scope per user: README + docs sync, hardening, stale-cache cleanup, workflow, license. (1) **Dual sqlite driver**: `-tags purego` → modernc.org/sqlite (FTS5 included) behind driver_cgo/driver_purego + dsn helpers; mattn stays the native default (D4 amended); store+server suites pass on both; all 8 release targets cross-compile with CGO_ENABLED=0. (2) **Workflow**: mdict-go-web's build-release.yml reused near-verbatim (ubuntu-only, matrix trimmed to mac arm64/amd64, linux amd64/arm64/armv7/armv6, windows amd64/arm64, purego build, version stamped from tag). Makefile `cross` now purego (works without C toolchains); `test-purego` target. (3) **Hardening**: dict.Open/OpenReader recover→error (corrupt-file matrix test across all formats: no panic escapes), server recover middleware, graceful SIGINT shutdown, port-in-use error with --port/lsof hint. Windows path audit: clean (virtual resource namespace is slash-based by design; all fs paths use filepath). (4) **`clean` command**: FindOrphans (source gone / hash superseded, paired media.db included), dry-run default, `-f` deletes; found 284MB of real orphans first run. (5) **LICENSE** GPL-3 + addlicense SPDX headers (D11). (6) **README.md** user-facing; SPEC/DECISIONS synced. Incident: ~10 source files got truncated to 0 bytes mid-session (cause unclear — concurrent user commit 4db9c3d + silenced gofmt suspected); recovered from user's commit + embedded copy in the built binary + context rewrites; full suite green on both drivers afterwards.

### P9 — Audit fixes & URL/MIME correctness — 2026-07-25 — COMPLETE (f06f9cf, 926e943)
Wrote `wudict/code-review.md` (measured RAM/CPU audit, bottlenecks C1–C10 + transport notes T1–T8) as the standing optimization backlog. Landed the risk-free tier: **D14** root-absolute `/res/…` everywhere (hardened `RewriteEntryHTML` — word-boundary attrs, srcset, inline `url()`, `<base>` stripped) fixing the srcdoc `res/{d}/res/{d}/…` doubling (OED `OED4.js`, AHD5 `wavs/*`); authoritative `webMIME` map (Go's `mime.TypeByExtension` gives `text/plain` for `.css`/`.js` on some platforms → strict-MIME rejection); `speexdec` resolved once at launch; HTTP server timeouts (ReadHeaderTimeout/IdleTimeout/MaxHeaderBytes, deliberately **no** WriteTimeout — it would sever NDJSON/SSE); `.spx` transcode single-flight keyed on the WAV cache (survives an in-process decoder swap); `RewriteEntryHTML` fast-path gate (C5 half); client `AbortController` cancels superseded searches (C7 half); gomdict per-lookup log/print noise removed. Favicon + CI native-cgo/purego release suffixes.

### P10 — Native-ingested overhaul — 2026-07-26 — COMPLETE (1abf35c, b0fcea1, 09418af)
Adopted **D15** (ingested = native, direct = preview) and built what it rests on. **D16** search modes redesigned to exact · prefix · contains · full-text: "fuzzy" retired (it was accent-insensitive prefix) and folded into `prefix`; real **contains** = FTS5 **trigram** index over `dict.Fold`ed headwords (≥3 chars MATCH, <3 LIKE), feature-detected at Open (`has_trigram`) so no schemaVersion bump and old `.text.db` keep working. **D17** dual-root registry: db dir is a first-class dictionary root, a `.text.db` whose source is gone becomes a standalone native dict (`StandaloneNativeDBs` + `server.native`), deduped by `source_path` existence; `FindOrphans` semantics **reversed** (data-loss fix — `clean -f` would have deleted naturalized dicts). Ingest hardening: upgrade no longer deletes the old db first (atomic temp+rename overwrites), fsync before rename, no-packable-media flagged (kills the pack-media flash-revert loop). Perf: C4 (`begin` streamed immediately, opens deferred into search workers via `search.StreamOpen`), C9 (mtime-invalidated `CacheBase` hash memo). Registry gained **longest-suffix matching** (`matchKey`) so `.dsl.dz` beats `.dz` and an unregistered `.dict.dz` companion is neither discovered nor opened by mistake. New format: **BGL** (`internal/format/bgl`) — streaming reader (metadata pass + lazy per-block second pass, tolerates BGL's absent/zero CRC), charset/language/POS tables via x/text, type-2 resources scanned lazily, DSL-style auto-ingest on first open.

### P11 — Preview-cost & in-process Speex — 2026-07-26 — COMPLETE (c4102d1, 5ff8638)
Validated against runtime reference readers (aard2 `Slob.java`, GoldenDict): real readers keep **no** in-memory headword index — pyglossary does only because it is a converter. So mdx/slob/stardict indexes (exact **and** fold) are now built lazily on first use (`sync.Once`); opens that serve resources, feed ingest, or fill the dropdown build none. OED SLOB 1.15M refs: open 1.1s→0.24s, meta-only RSS 554MB→243MB. C1: bounded FIFO (8) decompressed record-block cache in gomdict, open-per-miss (respects the .mdd fd budget), range-tree scan untouched. **D18** `.spx` decoded in-process on cgo builds: in-house pure-Go Ogg demuxer + vendored decode-only libspeex (BSD-3) compiled in-tree; `speexdec` becomes opt-in (`SPEEX_BACKEND=external`) and the automatic purego fallback. Validated vs the CLI on real MW11sound: max |delta| 1 LSB, SNR 93.4 dB, and our output honors the Ogg granule where speexdec leaves 40 padding samples. No new Go deps; make check green on cgo + purego.

### P12 — The library: private, consented, one folder per dictionary — 2026-07-27 — COMPLETE (uncommitted)
Investigation of three reported symptoms (a phantom `wudict:slob` row, the setup page never appearing, `--dict-dir <db dir>` triple-listing AHD5) traced them all to one design leak: D17 made the app's private cache a dictionary discovery root. Fixed as **D19** (phase 1) + **D20** (phase 2), user-directed. **D19**: `store` registers `text.db` (exact name) + `.text.db` instead of bare `.db` — a `media.db` sidecar can no longer open as a dictionary anywhere; `DICT_DIR == DB_DIR` is a hard startup error (canonical paths) and `dict.ExcludeDir` skips the library subtree wherever it sits; the library is listed only under `USE_CACHED` (default off), which restores `reg.Count()==0` as a correct first-run signal with no special-casing; the setup page gained a "Previously imported dictionaries" section (`GET /api/library`, badges, **Use these dictionaries**) plus an "also keep my N…" checkbox — three outcomes (folder / library / both), the click *is* the consent and persists `USE_CACHED=1`. **D20**: flat `<slug>-<hash8>.text.db` → `<db dir>/<source name>/{text.db, media.db, info.txt}`; naming mirrors the source file with a format tag on collision (`AHD5-2017 (mdx)`, never a hash); allocation is an atomic `os.Mkdir` **claim** so concurrent first ingests can't collide, while read paths use a non-creating `LookupDir` (browsing never litters the library); `info.txt` is a regenerated receipt whose `source` line is the **ownership record** — deliberately outranking meta's `source_path`, which a reader may report relative or empty (caught by a real failure: the fake-format fixture reported `x.fake` and orphaned its own folder); staleness moved from the file name into the meta (`SourceChanged`: size+mtime, hash only on mismatch → re-index in place, no `-oldhash` pile-up, "superseded" gone as an orphan class); `clean` is folder-aware; pre-D20 flat databases are **adopted** (renamed into folders at startup by `store.AdoptLoose`, never re-indexed or deleted) after the first cut wrongly made them `clean` fodder — which left a real library listing nothing and would have forced re-preparing data the app already had. Also: `--use-cached` flag, help/README/config template, panel shows the transferable **folder**, and a `TestMain` now pins `WUDICT_DB_DIR` for the server package after background auto-ingests were caught writing into the real `~/.wudict/db`. New tests: folder naming/collision/ownership/staleness/library listing, media.db-is-never-a-dictionary end-to-end, setup consent flow, bundle discovery + exclusion. Live-verified: guard, setup→consent→search, dedup with sources present, a handed-over folder used from a dictionary folder, `clean` dry run.

### P13 — Terminal output pass — 2026-07-27 — COMPLETE (uncommitted)
User-reported: a startup line naming DICT_DIR but never DB_DIR, then fourteen anonymous `ingest: N unresolved link targets (skipped)` lines with no way to tell which of 55 dictionaries each referred to. Fixed as a convention, not a patch (SPEC §6d, `internal/logx` package doc): library packages no longer print — `store.IngestLevelReport` **returns** a `Report{Entries, UnresolvedLinks}` and the CLI prints it while the server logs it via `logx.V` (D13 keeps background indexing silent); mdx per-entry read failures became verbose detail, a broken `.mdd` a `logx.Warn` naming the dictionary and the missing media; DSL/BGL first-open preparation now reads identically through a shared `store.ReportPrepared`. Every dictionary-scoped message carries `logx.Dict(name)` (`"Espasa Calpe": …`). New `logx.Warn/Status/Progress/ClearLine/Interactive`: in-place counters are suppressed when stderr is not a terminal (they collided with result lines on stdout when piped). Startup now prints the effective configuration — dictionaries folder, **library (DB_DIR) with prepared/in-use counts**, config file, address, .spx decoder, indexing mode, total served — each folder counted by what it contributed (a blended total made an empty dictionary folder look full when the library was in use), followed by one next-step line. Two real bugs found while verifying: the `Report` was never assigned (every count printed 0 — a silent no-op string replacement), and the Makefile expanded `LDFLAGS` before `VERSION`, stamping `-X main.version=` empty so the binary printed a blank version.

### P14 — Panel: enable all / none — 2026-07-27 — COMPLETE (uncommitted)
One master switch in the panel header, in the same right-hand column as the per-card ones, so the column reads as a single control surface: tri-state (`indeterminate` when mixed), Finder/Gmail semantics (mixed → all, all → none, none → all — a click on an indeterminate checkbox arrives as `checked=true`, so "mixed → all" needs no special case), label doubling as the count ("3 of 4 enabled" / "All enabled" / "None enabled" / "No dictionaries"). The `.en` switch styling was hoisted out of `.pd` so both use literally the same control — scoped to `.pd` it fell back to a native blue checkbox, caught by a rendering harness, not by reading the code. Header (title, index all, rescan, master switch) is now `position:sticky` so it survives scrolling a 55-dictionary list. Partial state = knob centred on a half-tinted track, never mistaken for the full-accent "on". Verified: state machine unit-tested in node against a DOM stub (all four transitions + the empty-library case), light/dark renders, and the live app (4 cards, correct initial label). **Deletion was scoped out by the user** — the design (trash-folder + Undo, overflow menu, inline confirm rather than `confirm()`, never touching source files) is recorded in the conversation but nothing was built; the enable/disable checkboxes stay a search-scope model, never a selection model.

### P15 — Several dictionary folders — 2026-07-27 — COMPLETE (uncommitted)
`DICT_DIR` became a list (D21): repeatable `--dict-dir`, `os.PathListSeparator` in the environment, TOML array in the file (scalar still valid — no migration). `config.ParseList`/`FormatList` + `SaveKeyRaw` round-trip lists through the comment-preserving config writer; `dict.DiscoverAll` walks roots in order and dedupes by canonical path, returning per-root `{New, Total}` so an overlapped folder reads "already listed above" rather than "no dictionaries found"; `Registry` keeps `dictDirs` + `Roots()` status; the startup block prints one line per folder with its own state (including "folder not found" for an unmounted drive, which is a warning, never fatal); the setup page gained add/remove folder rows, each validating itself, with the total driving the Save button and its label ("Use this folder" / "Use these folders"); `/api/setup` accepts repeated `path` params, refuses the library folder, and persists an array. `list`/`ingest`/`searchall` take folder lists too. Two bugs found by screenshotting rather than reading: a `.row` class collision with the library list, and `label.keep` (0,1,1) outranking `.hidden` (0,1,0) so the "keep my 0 previously imported" checkbox showed with an empty library. One real bug fixed in passing: `filepath.WalkDir` lstats its root, so a **symlinked dictionary folder discovered nothing at all** — `Discover` now resolves the root (links inside the tree still not followed). Tests: ParseList across all spellings + config round-trip, overlap/nesting/repeat/symlink dedupe with per-root counts, missing-root non-fatal, multi-folder setup save persisting an array, symlinked root discovery.

### P16 — Folders visible in-app; reveal in the file manager — 2026-07-27 — COMPLETE (uncommitted)
D22. `/setup` is now permanent and pre-filled (the folder editor, not just the first-run chooser, with Cancel once dictionaries are served); a collapsed **Folders & configuration** section at the foot of the panel shows each dictionary folder with its own status, the library with prepared/in-use counts, and the config file in effect — all click-to-copy, via new `GET /api/config`. `config.Load` now records each key's **origin**, so a `DICT_DIR` set by flag or environment shows "overrides the config file" in both the panel and the editor instead of letting a save silently do nothing. **Rescan moved** from the panel header into that section (user-directed: too expensive to sit next to the close button). `GET /api/reveal` opens the platform file manager with each OS's own wording — Reveal in Finder / Show in File Explorer / Open Containing Folder — guarded twice: the path must be one the UI already displays, and the request must come from loopback (revealing opens a window on the *server's* desktop). Two more class-scoping bugs caught by screenshots, both the same family as the earlier `.en` one: `.act` was scoped to `.pd`, so "Rescan folders" rendered as inert text, and the setup intro repeated every configured path in a sentence, pushing the controls off screen (now a one-line summary). Also fixed: the editor summed per-folder counts, promising 4 dictionaries where overlap meant 3 — it now asks the server for the deduplicated total. Tests: reveal authorization (allowed/denied paths, remote refused, unknown path refused), `within` prefix-sibling cases, per-OS label, `/api/config` shape incl. origin + loopback gating, `/setup` reachable with a summarising intro; plus a node DOM-stub run of the real `loadFolders` covering every row state.

### P17 — Lookup-link spellings — 2026-07-27 — COMPLETE (uncommitted)
`bword:`/`entry:` were already fully symmetric (both skipped by `RewriteEntryHTML` as real schemes — navigation, not resources — and both resolved by the same client branch in `index.html` and `frame.js`), but the client required the `//`: `bword:apple`, the slash-less form GoldenDict accepts and some MDX dictionaries emit, matched no branch and was inert (it looks scheme-shaped, so the bare-word fallback skipped it too). Both spellings are now accepted, and a trailing `#fragment` is dropped before percent-decoding — so `bword://word#sense2` looks up `word`, while an encoded `%23` inside a real headword survives. Extracted as `wordFromHref` in each of the two clients (the iframe bridge cannot import from the page); a single node table-test runs BOTH copies over the same 20 cases, so the duplicates cannot drift apart. Rewriter tests gained the slash-less and fragment forms to lock in that the server keeps its hands off them.

### P18 — Folder-level dedup — 2026-07-27 — COMPLETE (uncommitted)
Answering "do we guard against the same folder twice?": dictionaries were already safe (`DiscoverAll` dedupes by canonical file path — four spellings of one folder still served one dictionary), but the folders themselves were not: they showed as four rows in the startup block, the panel and the setup page, were walked four times, and could be written to `config.toml` twice. New `dict.DedupeDirs` collapses them with `os.SameFile` where both paths exist (catching symlink, case-variant, hard link and bind mount, none of which string comparison sees) and canonical strings otherwise (an unmounted drive is kept, and still deduped), preserving order and the first spelling; nested folders deliberately survive, since their overlap is reported per root. Wired at `NewRegistry`/`SetDirs` (covering flags, env, config file and setup saves) and in `resolveDirs` so nothing duplicate is persisted. Verified on the original scenario: four `--dict-dir` spellings now yield one row.

### P19 — "Keep my previously imported dictionaries" default — 2026-07-27 — COMPLETE (uncommitted)
Question "what happens if the user does NOT check it?" exposed two defects and settled a default. (1) The box was **write-only-true**: its state was sent only when checked, and the server only ever called `setUseCached(true)` — so clearing it could not undo an earlier yes, and on `/setup` it always rendered unchecked no matter the real setting. Now tri-state on the wire (absent = leave alone, `1` = on, `0` = off), and the box mirrors the current setting. (2) Default is now **checked** when the library is non-empty: the data exists because the user prepared it, checking only adds visibility and is reversible, and leaving it off silently hides working dictionaries whose files are on disk. Wording carries the consequence — "Keep using my N previously imported dictionaries — unchecking only hides them; nothing is deleted". Worth recording: the flag only governs prepared dictionaries the chosen folders do NOT account for; one whose source is inside a scanned folder keeps working with all its indexes regardless, since `PreparedFor` resolves the library folder from the source path. Test asserts on→off→omitted, including persistence of `USE_CACHED = "0"`.

### P20 — One act, one cog — 2026-07-27 — COMPLETE (uncommitted)
**Setup page harmonized.** It had three commit paths for two dimensions: "Use these folders" (+ a checkbox governing the library), "Use these dictionaries", and Cancel. The middle one posted with no `path` params, so typing a folder and then clicking the button that named what you wanted **silently discarded the folder** — verified in the handler before touching it. Folders and the library are not alternatives but two sources of one library, so the page now commits once: sections for each source, then a single footer (primary + "Back to dictionaries"). That label is deliberate: "Cancel" belongs to a modal or wizard step and means "abandon what I started", but nothing is pending here; "Close" implies dismissing something transient over your content, while /setup is a page at its own URL. It navigates, so it is an `<a href="/">` wearing a `.btn` class rather than a `<button>` running `location.href` — which restores ⌘/middle-click, the context menu, the status bar and the correct "link" role. The keep-checkbox moved beside the list it governs; the label names the deed and the enable rule accepts either source; one merged line states the outcome. A node stub exercises all five label/enable states and all four save URLs — the discard trap is now structurally impossible (paths are always carried) and covered by a test.
**Panel settings discoverable.** The "Folders & configuration" disclosure moved from the foot of the card list (undiscoverable past 55 cards) to one line under the master switch: cog + counts, no visible label (title/aria-label keep the name). Inline SVG rather than ⚙️ emoji — colour blob on macOS, flat on Windows, own baseline, cannot inherit currentColor. The first hand-written path read as a blob at 14px, so the gear is geometrically generated (8 teeth, evenodd centre hole) and was checked at 14/20/64px before landing; it turns 60° when open.

### P21 — Duplicate launch hands over — 2026-07-27 — COMPLETE (uncommitted)
D23. A second launch on a busy port now probes it (`GET /api/config`, 700ms, requires the new `Server: wudict/<version>` header), and when it is our own instance prints a loud ASCII banner — "IS ALREADY RUNNING -- this launch is doing nothing", what that instance is actually serving, an explicit "the options you passed were NOT applied", and, when the folders differ, the folders you asked for versus what you will see — then opens the browser at the running instance and exits 0. A foreign server on the port keeps the original error verbatim and opens nothing. Found while testing: the port was bound LAST, so a duplicate launch printed a full startup summary and auto-prepared a DSL index before discovering it had nothing to do; the listener is now claimed before any library or scanning work, and `Serve(ln)` replaces `ListenAndServe`. Rejected option B (an HTTP `/exit` kill endpoint): any web page can fire a cross-origin GET with an `<img>` tag, it would discard a long ingest, may not be the user's instance to kill, and races TIME_WAIT — the safe form would be a PID lockfile + SIGTERM behind `--replace`. Tests: probe recognises us, refuses a foreign server (even one returning a convincing body), refuses nginx, fails fast on a closed port; folder comparison is order-insensitive and symlink-aware; the Server header is present on every response including 404s.

### P22 — Cheap by default: compression + opt-in indexes + feature switches — 2026-07-28 — COMPLETE (uncommitted)
Measured first (40k-entry dictionary, 45.6 MB source): the "cheapest" database was 72.4 MB, of which **95% was article text** — every index together was 2 MB, trigram just 2.4 MB. So the disk complaint was about storage, not search. **(a) Bodies are now DEFLATE-compressed per row** (3.8x measured), NUL-sentinel discriminated so mixed/legacy rows read unchanged — no migration, no schema bump — with `NO_COMPRESS`/`--no-compress` to opt out; verified article-for-article identical against an uncompressed build. **(b) `store.Plan{FullText,Contains}`** replaces the single Level: the trigram index is built only when asked for (it costs about as much as the entire rest of a compressed index), and `PlanOf` keeps the legacy Level mapping. **(c) The panel's badges became switches**: exact/prefix were dropped (true for every dictionary, cost nothing — a badge always lit is decoration), leaving contains · full-text · media with their measured sizes, one click on, one click off, via `entry.setFeatures` which rebuilds only what changed and leaves untouched features alone. **(d) Stripping is offered only while the source exists**, which makes every switch reversible and removes the need for any confirmation prompt; a source-less dictionary shows locked switches with an explanation. Also: a skipped search mode now offers "enable X for this dictionary" inline (contains no longer claims to be "preparing"), auto-index builds only the find index and never demotes a user's choices, `ingest -contains` on the CLI, and `applyLibrarySettings` fixed a pre-existing gap where `wudict ingest`/`clean` ignored `DB_DIR` from config.toml. Net: default preparation 84.0 MB → **29.5 MB** (1.84x → 0.65x the source).

### P22.1 — AUTO_INDEX values renamed — 2026-07-28 — COMPLETE (uncommitted)
`AUTO_INDEX = fuzzy|off` became `on|off`: D16 retired "fuzzy" as a search mode two phases ago, leaving the setting named after something that no longer existed. `config.normalizeAutoIndex` accepts `on/1/true` and `off/0/false/no/none`, and still takes the legacy `fuzzy` as `on`, so an existing config.toml keeps working; `Config.AutoIndexEnabled()` replaces the scattered `!= "off"` comparisons. Startup now says what it means ("on — a headword index is prepared on first search; contains, full-text and media on request"), the setting is documented in `--help` (it is env/toml only, and was undocumented there), and the README section describing auto-indexing was rewritten around what D24 actually builds.

### P22.2 — Self-preparing formats stop imposing full text — 2026-07-28 — COMPLETE (uncommitted)
User spotted that BGL (and DSL) still got a full-text index automatically on first import — the last place a heavy index was imposed rather than chosen, left behind because those formats' auto-preparation predates D24 and was written when "prepare" meant "prepare fully". They have no native index, so their text.db must exist and must store article text to be readable at all; indexing that text is a separate question, and now a switch like everywhere else. First open of the measured 40k-entry dictionary: 29.5 MB → **17.2 MB**. `maybeAutoIndex` also stopped using `Caps().FTS` as a proxy for "already prepared" (untrue the moment a prepared dictionary carries no FTS index) and now relies on `ensureBaseIndex`'s own guard. Four tests carried the old premise and were corrected to assert the new contract: a self-preparing format must be *searchable* after first open, and must not build the opt-in indexes.

### P23 — Provenance row: one affordance, one copiable place — 2026-07-28 — COMPLETE (uncommitted)
User noticed the `+ n mdd` companion label looked like a link but did nothing. It was worse than inert: the panel emitted it with class `more`, the same class the **search-results** "more…" link uses, so that link's document-level handler matched it, called `preventDefault()` — cancelling the disclosure toggle — and then threw `TypeError: … reading 'parentElement'` looking for a `details.dict` that does not exist in the panel. Reproduced and fixed under a harness carrying the real `provenance()` and the real handler: before, `underlined? underline / JS ERROR / open = false`; after, `underlined? none / open = true`, no error. Fixed by *removing* rather than adding: the panel's label is now `.companions` (no shared name, no foreign handler, no fake link styling), the affordance moved to the whole `<summary>` row (hover tint + focus ring) because the whole row is what toggles, and the **source path was added as the first body row** — it lived only in the summary, so since the summary became a toggle it was copiable nowhere. Summary is now teaser + toggle; the body is the one place every path is copiable. Follow-up the same day: the summary shows the **file name only** (`mdx: AHD5.mdx + 1 mdd`) with the absolute path in its tooltip — a 60-character path repeated on every card is noise, and the full path is one click away in the body. The format prefix went too (2026-07-28): a card said "mdx" three times in four lines — the meta row, the `MDX:` prefix, and the `.mdx` the filename already ends in — with the last two adjacent inside one line. The meta row keeps it (it is the scannable line, and the only place that names the format for StarDict's `.ifo` or for a dictionary whose source is gone); the provenance summary drops it. The source-less branch deliberately keeps its `prepared:` prefix — that is a state, not a format, and it is the only signal that no original file is behind the dictionary. Applied to the source-less case too (`prepared: Old Dictionary`), which required adding the library folder as a body row there — otherwise shortening the head would have made that path uncopiable, the very bug just fixed on the source side.

### P24 — Performance audit and the first four fixes — 2026-07-28 — COMPLETE (uncommitted)
Full measured audit in **docs/PERF.md** against the real 105-dictionary / 18.1 M-headword corpus: unit costs per headword (300–500 B), per-format open/lookup/ingest peaks, and a server A/B showing prepared vs unprepared (17 MB idle / 33 MB peak vs 500 MB peak, 424 % CPU) — the model reproduces every reported symptom (18 GB, 2.5 GB idle, 1000 % CPU, 90 % search) with no unexplained residual. Root cause: `maybeAutoIndex` spawned an unbounded goroutine per dictionary while `Warm` (4) and `/api/dicts` (8) were bounded. Shipped **F1–F4** (D25): one limiter for background indexing with `INDEX_WORKERS` default **1** plus a separate foreground slot; superseded direct backends closed after a 10 s grace with `FreeOSMemory`; `Warm` no longer opens unprepared dictionaries; preparation no longer holds a second copy of the file open; optional `MEMORY_LIMIT`. Measured after: CPU 424 % → 154 %, resident-after-storm 474 MB → 172 MB, warm idle 17 → 13 MB; `TestIndexingConcurrencyIsBounded` pins the bound (1 → 1, 3 → 3). Documented as still open: an all-dictionaries search materialises every unprepared backend (~6 GB transient on the reference corpus) — PERF.md §6b lists the two candidate remedies for the next round.

### P25 — Bounded preview memory (S2) — 2026-07-28 — COMPLETE (uncommitted)
The residual from P24: an all-dictionaries search materialised every unprepared dictionary's headword map (~6 GB transient on the reference corpus). Now capped by `PREVIEW_MEMORY` (default 1 GB), weighed per dictionary at `EntryCount × 350 B` (the measured middle of 290–570 B), evicted least-recently-used by a 20 s janitor that never touches a backend used in the last 45 s and never runs on the request path — the anti-thrash rule matters more than the cap itself, since reopening costs 0.2–1.1 s each. `entry.open` moved off `sync.Once` to double-checked locking under its own mutex so an evicted backend is reopenable; closes wait a 10 s grace then `FreeOSMemory`. A prepared dictionary weighs 0 and is never evicted — and the predicate had to be "answers from a prepared database" (`SourcePath()`), because DSL and BGL embed a `*store.Store` without a wrapper and a type-switch on `*upgraded`/`*native` counted them as RAM-heavy (caught by the test, not by reading). Verified live at a 150 MB budget (evicted a 209 MB backend, logged) and by `TestPreviewEviction` (in-use survive any budget; idle over budget evicted; evicted reopens and still searches; prepared never evicted). Modelled first-run peak now ~1.5 GB against a measured 18 GB before P24.

### P26 — Resource handles budgeted too — 2026-07-28 — COMPLETE (uncommitted)
Closed the residual from P25: `upgraded.src`, the direct backend a *prepared* dictionary opens when a resource misses `media.db`, holds the same ~350 B per headword and was never released. It became a reclaimable of its own — `srcOnce` replaced by a mutex so it can reopen, weighed on open, timestamped on every resource fetch, released by the same janitor under the same never-if-recently-used rule — and the sweep now ranks both kinds on one LRU list (`reclaimable{used, bytes, free}`). Releasing it is invisible to the user: text comes from SQLite, the next resource request reopens the handle. Verified live (`released resource handle for es-en-wordref-audio.mdx (~9 MB)`) and by `TestResourceHandleIsEvictable`. **The test caught a bug in P25's own work**: `reopen()` swapped in the prepared view but kept the replaced backend's weight on the entry, so a freshly prepared dictionary looked RAM-heavy and could have been evicted — closing a Store that costs nothing to hold. Weight now follows the view at every assignment point.

### P27 — Browser history for cross-reference navigation — 2026-07-28 — COMPLETE (uncommitted)
Back did nothing after following a `bword:` link: the URL was kept in sync, but with `history.replaceState` for *every* search, so the app occupied exactly one history entry however many entries you visited — and there was no `popstate` listener, so even an existing entry would not have restored anything. The cause was reasonable: `doSearch` is debounced on typing, and pushing there would stack `c`/`ca`/`cas`/`casa`. Split the two cases with a one-shot `navRecord` flag: typing still replaces, `lookupWord` (link click, double-click, iframe bridge) pushes, `popstate` restores and records nothing. Identical consecutive URLs are not pushed; popping to a URL with no `q` aborts the in-flight search and clears the results. Verified by driving the real functions against a history stub: typing twice keeps depth 1, two link clicks give depth 3, re-clicking the same link does not grow the stack, Back → Back → Forward walks `vivienda → hogar → casa → hogar` re-running each search, and Back past the first query clears the screen.

### P28 — MDict sub-entries — 2026-07-29 — COMPLETE (uncommitted)
D26. Investigation first: the `entry://@…` buttons in LDOCE6 No-Voice point at real headwords — 95,162 of the dictionary's 160,544 "entries" (59 %) are `@`-prefixed sections, 65,382 are words. (A first probe looked at the wrong file in the same folder, `LongmanDictionaryOfContemporaryEnglish6thEnEn.mdx`, where the targets do NOT exist and the sections are inline with the dictionary's own `showAtLink`/`toggle` JS — worth knowing that both packagings exist.) Two changes: **inlining** — an `@` link now expands the section beside itself with a close control, no search/URL/history involvement; the iframe is same-origin so `frame.js` fetches and inserts it where the article's stylesheet and `toggle()` already live, and the duplicate `<link>` is stripped; shadow-DOM articles get the same path in `index.html`. **Hiding** — `@`-prefixed headwords are filtered out of prefix, contains, full-text, the fold fallback and the headword listing, in SQL so limits count visible rows and existing databases need no re-index; exact lookup still reaches them (that is what inlining uses), a bare `"@"` stays visible, and new ingests record `sub_entries` with `entry_count` meaning words. Verified against the real dictionary (prefix "collocations": `@collocations_000, @collocations_1, …` before → nothing after; exact fetch of `@examples_woman` returns 1,893 bytes with `/res`-rewritten CSS) and by node stubs for the toggle (insert, strip `<link>`, close on second click, "(not in this dictionary)" when unresolved).

### P29 — Loose sibling assets for MDX — 2026-07-29 — COMPLETE (uncommitted)
MDict serves files next to the `.mdx`; we only ever looked inside `.mdd`, so a repack that ships its stylesheet loose (LDOCE6: `LDOCE6.css`, `entry.js`, no `.mdd` at all) rendered unstyled. `Dict.Resource` now falls back to the dictionary's own directory on a packed miss. Measured before deciding: `stat` costs **1.1 µs** hit or miss and a `readdir` 16–26 µs, against ~100–500 µs for the HTTP round trip that carries it — so the two proposed optimizations were declined with numbers: an extension filter is not needed for speed (though one is kept for *safety*), and negative caching would trade 1 µs for staleness plus an unbounded map keyed by untrusted article strings. Packed hits never touch the disk, so the hot path is unchanged. Guards: original-case name (the MDD index key is lower-cased, and `LDOCE6.css` ≠ `ldoce6.css` on Linux), containment check after `path.Clean`, and an asset-extension allowlist. Verified live over HTTP: `LDOCE6.css` and `entry.js` → 200 with correct MIME, `nope.css` → 404, `secrets.env` → 404 though it exists. **Packing extended** the same day: `pack media` now also stores the loose files articles reference (`store.ReferencedAssets` scans the prepared bodies — no source re-parse — keeping only relative, scheme-less, non-traversing names, capped at 20k distinct), so a packed dictionary keeps its stylesheet and scripts once the source is gone. Referenced-only, never the folder: `~/Downloads/Language/mdict` holds 49 dictionaries side by side, and a sweep would pack a neighbour's CSS. `Media.Resource` now retries `COLLATE NOCASE`, since MDD names are indexed lower-cased while loose files keep their real spelling.

### P30 — Audio clicks inside script-bearing articles — 2026-07-29 — COMPLETE (uncommitted)
Clicking a speaker icon in LDOCE6 navigated the iframe to the .mp3, replacing the article with a native media player. Cause: the dictionary's own `entry.js` attaches `click` → `stopPropagation()` to the speaker `<img>` (so playing does not also toggle the surrounding accordion), and our handler was registered on `document` in the **bubble** phase — the event died at the image, so the default navigation ran. Fixed by registering in the **capture** phase, which runs on the way down before any in-article handler; the handler still only acts on anchors it recognises, so the dictionary's toggles are untouched. Verified in a headless browser with a harness reproducing the exact shape (play link wrapping an `<img>` carrying the dictionary's `stopPropagation`, inside an accordion): navigation prevented ✓, audio played ✓, accordion NOT toggled ✓, cross-reference links still intercepted ✓.

### P31 — Module renamed to `.../wudict`, second binary deleted — 2026-08-02 — COMPLETE (uncommitted)
D27's "two binaries" shipped duplication as a workaround for a naming choice: `./main.go` and `cmd/wudict/main.go` were byte-identical delegations to `internal/cli`, existing only because the module path ended in `wuweidict`. Fixed at the source (D28) — module path is now `github.com/wuweidict/wudict`, `cmd/` is deleted, `CMD := .`. Touched: `go.mod` + every import, Makefile (`BINARY_LONG`/`CMD_LONG` gone, `install`/`clean` collapsed, version-stamp comment), both workflows (`-o "$BINARY" .` and the ldflags path), `.gitignore` (dropped `/wuweidict`), `cli.go` identity constants (`SiteURL`/`RepoURL` → `/wudict`), the About/setup footers, the systemd `Documentation=` URL, and the README build section (which also had two stale `build-release.yml` references — the files are `build-cgo.yml` and `build-purego.yml`). `make check` green (14 packages), purego build green, `wudict --version` stamps correctly. **Requires renaming the GitHub repo to `wudict`** and re-pointing local remotes; `go install .../wuweidict@latest` breaks permanently by design.

### P32 — `no such module: fts5` on `go install` — 2026-08-02 — COMPLETE (uncommitted)
Reported against the installed binary while `make build` worked. Diagnosed statically: driver choice keyed on `!purego`, so no-tags ⇒ mattn, and mattn omits FTS5 without `-tags sqlite_fts5` — a tag this repo never reads and only the Makefile supplied, while `store`'s schema creates `entry_fts` unconditionally. Constraints rewritten to `sqlite_fts5 && cgo` / `!sqlite_fts5 || !cgo` (D29), which is exhaustive: every tag/CGO combination now yields exactly one driver and all of them have FTS5. Verified 7 combinations via `go list`; tests green tag-less (the previously broken case), under `make check` (13 packages, cgo), and under `make test-purego`. README promotes `go install -tags sqlite_fts5 …@latest` as recommended with the bare form as fallback; Makefile and CLAUDE.md comments corrected — cgo is the Makefile's default, not the toolchain's.

### P33 — Startup readiness UX — 2026-08-02 — COMPLETE (uncommitted)
D30. `/api/dicts` converted from `wg.Wait()`+`writeJSON` to NDJSON (`begin`/`total` → `dict` rows in completion order → `end`), reusing `handleSearch`'s `Flusher`/`json.Encoder`/`writeLine` idiom with a mutex added because these workers write concurrently; `handleRescan` still delegates unchanged. Client gained an explicit `boot`/`partial`/`ready`/`failed` phase: the search controls ship `disabled` in the markup and are released on the **first** arriving dictionary, a large indeterminate spinner plus rotating Cook Ding (庖丁解牛) messages fill `#out` until then, and `#status` carries `N of M ready`. The false `⚠ All dictionaries are disabled` is now gated on a *settled, non-empty* list — mid-stream it reads as loading and re-runs itself at `end` — and `setStatus` gained an owner token so the loader retracts only its own message (the old `if(!lastQuery)` cleanup was why the warning stuck). Also: 20 s no-`begin` watchdog into a retryable `failed` state, `renderPanel` repaints debounced (was O(n) per arriving row), preference-order normalization deferred to the complete set (mid-stream it would have dropped un-arrived ids), and the panel header says "Still loading…" instead of "No dictionaries". Tests: the 10 `getJSON(t, s, "/api/dicts", …)` call sites moved to a `getDicts` NDJSON helper that asserts `begin`…`end` on every call, plus `TestDictsStreamShape` pinning frame order, content type, and `begin.total == reg.Count()`.

### P34 — `⚡ index all` reworked into a priced, drawer-level offer — 2026-08-02 — COMPLETE (uncommitted)
D31. The panel-header button is gone; `#folders .facts` gains a muted "Full-text search for all…" trigger next to "Rescan folders", expanding an inline `.bulkbox` that states the extra-not-a-fix framing, the candidate and entry counts, the one-at-a-time cost, current prepared-data size, a corpus-calibrated added-size estimate (omitted when no already-full-text dictionary exists to calibrate from), and reversibility — then `Index N dictionaries` / `Cancel`. `ftsCandidates()` now requires `d.source`, so source-less dictionaries are no longer queued for an ingest that cannot succeed, and the per-iteration progress label falls back to a fresh detached node instead of one shared element that `runIngest`'s never-cleared `dataset.busy` would have jammed after the first dictionary. Keyboard activation added for the `role="button"` span. Verified: `node --check` on the extracted script, `make check` (14 packages, cgo) and `make test-purego` green.

### P35 — Config file renamed and relocated — 2026-08-02 — COMPLETE (uncommitted)
D32. `config.toml` → `wudict.toml` everywhere, **no legacy fallback** (user-directed): the old name is not read in any location. Search order is now `--config`/`CONFIG_PATH` → `<exe-dir>/wudict.toml` → `~/.wudict/wudict.toml` → `/etc/wudict/wudict.toml`; the bare working-directory candidate is deleted along with the defect it carried (a stranger's `config.toml` became our `SaveKey` target). `EnsureConfigFile` lost the writability probe and its exe-dir preference — it writes `~/.wudict/wudict.toml` and nothing else — while a `wudict.toml` the user places beside the binary still wins the search and takes the saves, so portable mode survives as an explicit declaration rather than a guess. `Config` gained `Portable` and `Shadowed`; `printStartup` names the config file on every run (marking portable, listing ignored lower-priority files) and `cfg.Source` is now updated after first-run creation, which is why that run used to print `(none — built-in defaults)`. `loadFile` returns a `resolved` struct instead of three values. Testability: `exeDir`/`homeDir`/`systemDir` seams with an `isolate(t)` helper, so the suite can no longer see the developer's real `~/.wudict` or `/etc` — new `TestConfigLocation` covers creation target, idempotence, portable-beats-home, home-beats-system, the working directory being unsearched, and explicit-path-must-exist. Touched besides: `--help` (search order + a PORTABLE MODE section), README config section, `.gitignore` (`/wudict.toml`), and comment wording in `internal/dict`, `internal/server`, `setup.html`. `make check` green (14 packages, cgo), `make test-purego` green.

### P36 — Server-side enabled/disabled state and order — 2026-08-02 — COMPLETE (uncommitted)
D33. New `internal/server/prefs.go`: `Prefs` (load / `Off` / `Snapshot` / `Replace`, temp-file+rename saves, an empty path meaning in-memory) plus `GET`/`PUT /api/prefs`, wired through a variadic `NewRegistry(dirs, useCached, opts...)` + `WithPrefs` so the state is in place before `Warm` runs — `Warm` now skips disabled dictionaries, which is the bug D33 was really about (turning one off previously saved nothing). `statePath()` in `cli.go` puts `state.json` next to the resolved `wudict.toml`, falling back to `~/.wudict` and finally to memory when there is no home directory. Server-side `heal` re-attaches records by id → absolute path → unique file name (uniqueness required on both sides, so the library's many `text.db` entries can never swap), persists the repair, and keeps unmatched records instead of dropping them; `merge` takes paths from the registry rather than the client and retains dictionaries the client could not see. Client: `wudict_order`/`wudict_disabled` replaced by `loadPrefs()` before `loadDicts()` and a 400 ms-debounced `savePrefs()` that skips no-op payloads, refuses to write when the initial read failed, and adopts-then-erases the legacy keys once when the server reports `exists: false`; `wudict_theme`, `wudict_wide` and `wudict_dict` stay local by design. Tests: round-trip through the file including a restart, retention of an unmounted drive's records, healing of a moved dictionary with a durable write-back, ambiguous file names left alone, malformed file ignored, and path-less in-memory operation. `make check` green (14 packages, cgo), `make test-purego` green, `node --check` on the extracted script.

### P37 — `fold_version` — 2026-08-02 — COMPLETE (uncommitted)
D34, the first item out of the new `docs/OPEN.md` backlog (O1). `dict.FoldVersion = 1` sits beside `Fold` with the bump obligation stated in its doc comment; `IngestPlan` records it in `meta`; `store.FoldStale(meta)` and `Store.ContainsStale()` report a mismatch, with `foldVersionOf` defaulting an absent key to **1** so no existing library is declared stale. Surfaced as `containsStale` on `/api/dicts` through both build paths — the cheap `ReadMeta` one and the full-open one (via an `interface{ ContainsStale() bool }` assertion, with `upgraded` delegating to its `Store`). `setFeatures` gained a fourth rebuild case (`want.Contains && staleFold`), so re-requesting a feature you already have is the repair; the panel chip stays *on* with a dotted border and a `⟳`, and its click sends `contains=1` instead of `contains=0`. The audit behind it corrected the scope recorded in OPEN.md: `Fold` reaches disk **only** at `ingest.go:198` (the trigram index) — `entry_fts` folds with SQLite's own tokenizer, `idx_entry_w` is raw NOCASE, and the direct backends rebuild fold indexes on open — so one index goes stale, not three, and neither disabling contains nor auto-rebuilding is warranted (see D34). Tests: `fold_version` is written; a 7-case table for `FoldStale` covering grandfathering, older/newer/unparseable versions and the no-trigram cases; `TestStaleFoldDetectedOnOpen` ages a real database and asserts contains still *answers*; and `TestStaleFoldIsReportedAndRebuildable` drives the whole path over HTTP — report, keep serving, re-request, repaired. A `sqliteDriver()` test helper reads the registered driver name from `sql.Drivers()`, since `store.driverName` is compile-time private and differs by build tag (D29). `make check` green (14 packages, cgo), `make test-purego` green, `node --check` on the extracted script.

### P38 — Expand-all results toggle — 2026-08-02 — COMPLETE (uncommitted)
D35. A `⊞`/`⊟` button joins `◐ ⇔ ☰` in the header; `applyExpand()` keeps glyph, `title`, `aria-label`, `aria-pressed` and the accent tint in one place and is called once at init so the markup's default cannot drift from the JS. Clicking flips an in-memory `expandAll` and opens/closes the `details.dict` already on screen rather than re-running the search; `renderSlot` sets `det.open=expandAll` for rows that arrive later; the auto-open-highest-preference block is skipped while it is on, with `openedIdx` left `null` so a late lower-index dictionary cannot snap a section shut mid-read. `details.dict[open]>dl` gained `content-visibility:auto` + `contain-intrinsic-size` — articles are constructed eagerly regardless of the accordion state, so opening ~100 of them costs layout and paint, which the browser now skips for offscreen sections (this helps the default one-open mode too). **No persistence of any kind** — no localStorage, no `state.json`, no API, no Go, no server tests (see D35 for why nowhere beat both). 40 lines of HTML/CSS/JS. README tips line updated. `node --check` on the extracted script, `make check` green (14 packages, cgo).

### P39 — Toolbar/content alignment — 2026-08-02 — COMPLETE (uncommitted)
D36. Extracted a `.col` class (`width:92%; max-width:800px; margin:0 auto; padding-inline:var(--sp-2)`, plus the `max-width` transition so the bar eases with the cards) and gave it to both `<main class="col container">` and `<form id="frm" class="col">`; `body.wide .col` and the ≤600px `.col{width:auto}` now state the wide and mobile cases once instead of twice. `.container` keeps only `padding-bottom`; `.bar form` keeps only `display:flex`/`gap`/`justify-content`; `.bar` drops its horizontal padding (full-bleed, and a second inset would inset the form twice — the trap that made the obvious one-line fix wrong at ≤600px). Added `.bar form .tools{margin-left:auto}` with the class on `#themeBtn`, splitting the row at the query-controls/view-controls seam so both ends stay pinned to the column and further buttons consume slack rather than widening the bar. Root cause was pre-existing and unrelated to D35's button: `.container` carried a `--sp-2` gutter inside its 800px border-box and `.bar form` carried none, so the bar ran ~9.9px wider per side at every viewport above 600px — concealed until five controls filled the row. 🔍 kept by decision (D36). `node --check` on the extracted script, `make check` green (14 packages, cgo).

### P40 — Sticky, self-condensing section headers — 2026-08-02 — COMPLETE (uncommitted)
D37. `details.dict>summary` gains `position:sticky;top:var(--stick);z-index:2` and an explicit `background:var(--bg-card)` (without it, article text scrolls visibly through the header). `details.dict` changes `overflow:hidden`→`clip` — the prerequisite, since `hidden` would have made the section its own non-scrolling scrollport and killed the stick silently. `details.dict[open]>summary` is the compact form (`.8125rem`/`1.25`/`.25rem` block padding, count to 11px; ~39px → ~25px) with a 160ms transition, chosen over stuck-detection because `[open]` is free, needs no sentinel/observer/scroll handler, and fires on a user click rather than during scroll. Summary horizontal padding moved `var(--sp-2)`→`.62rem` so the type change never shifts the name's left edge. `--stick` added to `:root` as a pre-script fallback, published from the bar's real `offsetHeight` by a `ResizeObserver` (not a `resize` listener — the bar rewraps when the dictionary `<select>` grows with loaded names, which fires no resize event), and dropped to `0` by a new `body.barhidden` class toggled alongside `.bar.hidden` in the existing scroll handler, transitioning on the bar's own `.28s`. Verified no clipping ancestor remains on the path `body → .col.container → #out → box → details` (the surviving `overflow:hidden` is on `iframe.article`, a descendant of `dd`), and that P38's `content-visibility:auto` sits on `dl`, a sibling of the summary, not an ancestor. `node --check` on the extracted script, `make check` green (14 packages, cgo).

### P41 — Scroll corrections on section open/close — 2026-08-02 — COMPLETE (uncommitted)
D38. A second `#out` click listener matches `details.dict>summary` (skipping `.more`/`.upgrade`, which run their own handlers and `preventDefault` the toggle but only *after* this nearer listener), records `opening=!det.open` before the default action lands, and decides in a `requestAnimationFrame` once the new height is laid out: opening scrolls only when the section's top is below `innerHeight*0.5`; closing scrolls only when its top is above the bar. Both use `scrollIntoView({block:"start"})` against a new `details.dict{scroll-margin-top:var(--barh)}`, downgraded to `behavior:"auto"` under `prefers-reduced-motion`. The `--stick` custom property was split into `--barh` (the measured bar height, what `measureBar`'s ResizeObserver now writes) and `--stick:var(--barh)` (where a stuck header rests, still zeroed by `body.barhidden`) — because every correction scrolls upward and therefore ends with the bar back on screen, so it must aim at `--barh` and never at a `--stick` that is momentarily `0`. Deliberately NOT hooked to the `toggle` event, which also fires for `renderSlot`'s programmatic `det.open=expandAll` and would have jolted the page on every arriving row. `node --check` on the extracted script, `make check` green (14 packages, cgo).

### P42 — In-article `bword://#fragment` anchors — 2026-08-03 — COMPLETE (uncommitted)
D39. Added `fragFromHref` + `anchorTarget` + `revealAt` to `index.html` and the mirrored `fragFromHref` + `jumpToFragment` to `frame.js` (the two files' link logic is kept in step by comment contract). The click handlers now fall through from "no word" to an in-article jump instead of swallowing the click. Shadow-DOM articles resolve via `a.getRootNode().getElementById` (a `ShadowRoot` inherits it from `DocumentFragment`) and scroll directly; iframe articles post `{t:"anchor",fid,y}` because `renderFrame` sizes the iframe to its content, leaving it with nothing to scroll — the parent adds the iframe's viewport position and moves the page. `revealAt` subtracts the bar height *and* the section's sticky summary height (D37), which neither `scrollIntoView` nor `scroll-margin-top` can account for, and honours `prefers-reduced-motion` per D38. Verified against the live server: the served article for "Artificial Intelligence" (170,609 bytes) contains both `href="bword://#HistAI"` and `id="HistAI"`, and carries `<script>`, so this dictionary takes the iframe path. Word-plus-fragment links (`bword://Artificial Intelligence Notes#note-9`) still ignore the fragment — recorded as a known gap in D39, not a regression. `node --check` on both scripts, `make check` green (14 packages, cgo).

### P43 — Cross-reference scoping and word+fragment anchors — 2026-08-03 — COMPLETE (uncommitted)
D40, completing D39's known gap. `lookupWord(w,opts)` gained `{from,frag}`; the click handlers in `index.html` and `frame.js` now pass the article's dictionary id and the href's fragment (frame.js sends them on its `{t:"lookup"}` message). New one-shot state beside `navRecord` — `scopeOnce` / `anchorOnce` / `lastScope` / `widenedFrom` — consumed at the top of `_doSearch` before its first `await`, so the history wrapper can still read which scope ran. `dictSel` becomes `scope||$("dict").value`, so a scoped lookup takes the existing single-dictionary path (`perDict=25`) with no server change. On `end`: `scope&&!anyShown` re-runs once unscoped (`navRecord="replace"` so the URL sheds its now-false `from`) and reports it via `widenNote` on the *next* search's end, because the re-run's own "Searching…" would have overwritten a status message; `scope&&anyShown` appends `scopeNote` with a one-click *Search all dictionaries* (handled by a third `#out` click listener). URL/history/`popstate`/`applyURL` carry `from=<id>` separately from `dict=`, so restoring a link-followed view never rewrites the selector. Fragments: `renderSlot` consumes `anchorOnce` for the matching dictionary — shadow-DOM articles resolve via `anchorTarget(roots[0],frag)` in a `requestAnimationFrame` and use D39's `revealAt`; iframe articles get `data-frag` through `renderArticle`→`renderFrame`→`frameDoc` and jump themselves in frame.js's load handler, ordered strictly after `post()`. Double-click lookup left global in both renderers, by decision. Verified live against `eng-eng-stanford-ep`: "The Turing Test" and "Cognitive Science" each resolve to one headword in that same dictionary, a nonsense word returns `hit` with `results:[]` (so the widen path fires), and "Artificial Intelligence Notes" exists there and contains `id="note-9"`. `node --check` on both scripts, `make check` green (14 packages, cgo). README cross-reference sentence updated.

### P44 — Reference parsing refactor — 2026-08-03 — COMPLETE (uncommitted)
D41, fixing a reported truncation and the class behind it. `wordFromHref` + `fragFromHref` (two half-parsers over one string, with the KIND smuggled through the return value as null/""/leading-@) replaced by a single `parseRef(href) → {kind:"lookup"|"anchor"|"sub", word, frag} | null`, mirrored in `frame.js`; the click handlers now read the kind and dispatch, never re-derive it. `lookupWord` split into `searchFor(term,{scope,frag,record})` (verbatim term — author links, restored URLs) and `lookupSelection(text)` (tidies picked prose, then delegates); the old shared cleaner was truncating every multi-word headword. The iframe message is typed by provenance — `{t:"ref"}` vs `{t:"pick"}` — instead of inferring it from a missing `dict` field. Parsing is by string position with the split at the first literal `#` before decoding (so `%23` stays inside a headword), never via `URL()`, because `bword://Some Headword` is not a valid URI. Also: plain `<a href="#foo">` now works in shadow-DOM articles (previously it dirtied the page URL with an unmatchable hash; iframes keep native handling), `#` restored to the bare-word fallback's exclusion list so an out-of-article `#foo` cannot be searched as a headword, and slob bare-word links are scoped like other references. Verified with a node harness: 15 hand-built cases (multi-word headwords, `%23` in a headword, encoded `&`, `@` sub-entries with and without slashes, `d:`/`x:`, non-references) all pass, and over the 123 distinct hrefs in the real "Artificial Intelligence" article the parser yields 59 lookups / 26 anchors / 38 non-references — with **59 terms the old cleaner would have truncated** and **72 fragments it discarded**. `node --check` on both scripts, `make check` green (14 packages, cgo).

### P45 — `keys` panic, and the Keywords contract — 2026-08-04 — COMPLETE (uncommitted)
D42. `dict.Keywords`' doc comment now states the `n<=0` / negative-offset / past-the-end behaviour, and `dict.KeywordRange(total,offset,n)(lo,hi,ok)` resolves the window for every backend — mdx, slob and stardict lost their triplicated `min(offset+n,len)` arithmetic (the source of `makeslice: cap out of range` on `-n -1`, and of a silent overflow on a huge n), and `store` lost the `clamp(n)` that was turning "no limit" into maxLimit's 500. The bound is computed against the remaining count, so `offset+n` can never overflow into a negative slice index. `cmdKeys` defaults `-n` to 0 (= all) instead of 50, writes through a `bufio.Writer`, and returns on a write error so `| head` closes cleanly. New `TestKeywordRange` (12 cases: plain windows, n at the boundary, n<=0, negative offset, empty dictionary, `math.MaxInt` for both arguments) and `TestKeywordsHonoursTheNoLimitContract` for the prepared backend. Verified against real dictionaries: the reported `keys -n -1` on a 92,685-entry SLOB now prints all 92,685 (as do `keys` and `-n 0`), `-n 5` and `-offset 2 -n 3` window correctly, a negative offset counts as 0, an offset past the end prints nothing; MDX prints 3,108 keys matching its own `info` entry count; the prepared backend prints all 44,276 for both `n=0` and `n=-1`, 3 for `n=3`, and 44,266 for `-offset 10`. `make check` green (14 packages, cgo), `make test-purego` green.

### P46 — Article references parsed with a tokenizer — 2026-08-04 — COMPLETE (uncommitted)
D43. New `internal/htmlref`: a `Rewriter` over `golang.org/x/net/html`'s WHATWG tokenizer with `URL`/`Drop` hooks, a `Ref{Site,Tag,Attr,URL}` handed to policy, `Refs` for collection, and `Clean` for RFC-3986 repair — the package doc carries the token/state/action table. Untouched elements are re-emitted from `Raw()`, so well-formed markup round-trips byte-for-byte (pinned by tests over synthetic cases and, optionally, real article fixtures via `WUDICT_TEST_HTMLDIR`); `rewriteSrcset` gained a not-changed early return after the round-trip test caught it re-spacing candidate lists. `server/rewrite.go` reimplemented on it: `attrRef`/`srcsetRef`/`styleAttrRef`/`styleBlockRef`/`baseTagRef`/`cssURLRef` all deleted, `<base>` dropped via `Drop`, and a new element-aware `isResourceRef` (documented table) that stops rewriting cross-reference hrefs into `/res/` URLs. `store/media.go`'s duplicate `assetRef` replaced by `htmlref.Refs`. `dict.IsAssetName` extracted so mdx's loose-file security allowlist and the rewriter's link-vs-file test cannot drift. `slob.Resource` now calls `ensureExact`/`ensureFold` before reading them — it never did, so resources 404'd on any Dict opened without a prior lookup, which is exactly the prepared-dictionary resource fallback. `rewrite_test.go` rewritten around whole elements (the old bare `href="…"` fragments encoded the regex's text-matching assumption) with cases for unquoted values, the dropped opening quote, script/comment/prose non-references, `<span href>`, and byte-preservation. Verified against the reported article: `plaintiff__gb_1.ogg` and `plaintiff__us_1.ogg` now rewrite to `/res/…`, `oald10.css`/`oald10.js` still do, `defendant`/`defendant_e`/`collocations/plaintiff`/`@topic_…` are left as cross-references, and `wudict res` retrieves `plaintiff__gb_1.ogg` as `audio/ogg`. New dependency `golang.org/x/net` (pure Go; `make test-purego` green). `make check` green (15 packages, cgo).

### P47 — Schemeless cross-references in the iframe renderer — 2026-08-04 — COMPLETE (uncommitted)
D44. `frame.js` gained the bare-word branch `index.html` has carried since D41: a schemeless, non-rooted, non-anchor href now posts `{t:"ref", w, dict}` (scoped, per D40) instead of letting the browser navigate the sandboxed frame to a relative URL under `about:srcdoc`. Exclusions mirror index.html's exactly — real scheme, rooted path, in-page anchor (followed natively inside an iframe, unlike inside a shadow root), and our `/res/`, `/assets/` prefixes — and the capture-phase `stopPropagation` guard is applied as for other reference clicks so dictionary scripts cannot re-navigate. Verified by lifting each renderer's real parser and branch conditions into a node harness and dispatching the article's actual hrefs through both: `defendant` (a real slob key) and `@topic_law-and-justice?level=c2` (the `?` is part of the key — confirmed against the key list) resolve as scoped lookups in both, `#relatedentries` is an in-article jump, `/res/…ogg` plays, real schemes fall to the browser. GoldenDict-ng's sources were read for parity: it gets the same outcome by serving articles under `gdlookup://` so relative links inherit the scheme (`articleview.cc:1186`) plus an Aard-level rewrite to `bword:` (`aard.cc:350`); three of its behaviours were reproduced against the real markup and rejected — `https://` links rewritten to `bword:` by a `(?!ttp://)` guard that misses the `s`, audio links on `<a>` turned into word lookups, and the same quoted-attribute assumption D43 removed. `node --check` on both scripts, `make check` green (15 packages, cgo).

### P48 — Content-addressed asset URLs — 2026-08-04 — COMPLETE (uncommitted)
D45. `assetTag()` (8 hex of sha256) added to `server.go`; `page()` now substitutes `{{FRAMEJS}}` and `{{MARKJS}}` alongside `{{VERSION}}`, and index.html requests `/assets/frame.js?v={{FRAMEJS}}` and `/assets/mark.min.js?v={{MARKJS}}`. Both scripts moved to a shared `serveScript` helper and gained `immutable`; `/` gained `Cache-Control: no-cache`, since the page is the only thing that names the hashes and a heuristically-cached one would re-introduce the skew. Diagnosed from the reported Merriam-Webster symptom by elimination: the raw article carries `entry://complainant`, `complainant` and `suer` are real headwords, `RewriteEntryHTML` leaves the scheme untouched, and `htmlref` round-trips the article byte-identically — leaving a stale cached frame.js posting D41's old `{t:"lookup"}` message to an index.html that listens for `{t:"ref"}`/`{t:"pick"}`, which is why only script-bearing (iframe-rendered) dictionaries were affected. New `assets_test.go` pins that the page names both scripts by content hash, that no placeholder survives substitution, that the hash depends on the bytes, and the two cache headers. Also noted while diagnosing: re-serialising a tag decodes numeric character references in attribute values (`&#10;` → a literal newline), which is semantically identical and affects only tags whose value actually changed. `make check` green (15 packages, cgo), `make test-purego` green, `node --check` on both scripts.

### P49 — `keys`/`res` accept an .mdd directly — 2026-08-04 — COMPLETE (uncommitted)
D46. New `internal/dict.RegisterInspectable` + `discoverableFor`: files openable by explicit path but invisible to folder scans, so `.mdd` can be opened without becoming a dictionary (`openerFor` tries file names, then dictionary suffixes, then inspect-only containers, so a real format always wins). New `internal/format/mdx/mdd.go` — `OpenMDD` builds an `MDD` implementing `dict.Dictionary` over one resource container via the existing `openIndexed`: `Keywords` lists names (sorted, forward-slash, original case) through `dict.KeywordRange`, `Resource` fetches case-insensitively, `Exact`/`Prefix` return nothing and `Caps` is empty, because an .mdd has files rather than headwords. Registered from mdx's `init` as inspectable. `--help` and README updated. Verified on a real 54,164-entry .mdd: `keys` lists all of them, `-n`/`-offset` window correctly, `info` reports `format: mdd`, and `keys -n 1 | res` round-trips to `mime: audio/ogg`; across the real dictionary tree 39 `.mdd` files remain undiscovered while 51 `.mdx` are still found. Four new tests (opens with no .mdx present, keys→res round-trip including an upper-case lookup, the D42 window contract plus sort order, and non-discovery of three companions). `make check` green (15 packages, cgo), `make test-purego` green.

### P50 — Shadow-DOM articles get their embedded fonts — 2026-08-05 — COMPLETE (uncommitted)
D47. `index.html` gained `hoistShadowFonts(root)`, called from `renderArticle` after the host is connected (a `<link>` in a disconnected tree does not begin loading): each `<style>`/`<link rel=stylesheet>` in the shadow root is walked via CSSOM and every `CSSFontFaceRule` is copied into one document-level `<style id="wudict-fonts">`. `collectFaces` descends through `@import` (`.styleSheet`) and the grouping rules `@media`/`@supports`/`@layer` (`.cssRules`), and swallows the `SecurityError` a cross-origin sheet throws so a CDN-linked font degrades instead of breaking the render. Linked sheets are cached by absolute href and faces de-duplicated by rule text (two dictionaries may ship one font); inline `<style>` is walked every render, which is strictly cheaper than the `innerHTML` parse already paid for the same bytes. Families in the chrome's own stack (`-apple-system`, `helvetica`, `arial`, …) are skipped, since a hoisted face is document-global and would otherwise restyle the app. A `<link>` whose sheet has not loaded gets a one-shot `load` listener. The iframe renderer is untouched — an iframe is a real document and its faces have always worked. Diagnosed by dogfooding: the article links `ahd5.css` and carries no `<script>` (so it takes the shadow path), and `wudict res AHD5.mdx ahd5.css` returns 3,015,066 bytes containing four `@font-face` rules — `'Minion New'`, `'Minion New Bold'`, `'Minion New Italic'`, `'Minion Pro Italic'` — each with the TTF inline as base64, confirming the font was delivered and discarded rather than missing. `node --check` on the page script, `make check` green (15 packages, cgo).

### P51 — Folders panel: paths, grouping, wording — 2026-08-05 — COMPLETE (uncommitted)
D48. `configInfo.Home` added (`os.UserHomeDir`, display-only). In `index.html`: new `tildePath` + `pathHTML` helpers render `~/Downloads/Language/mdict` with a `<wbr>` after each separator, while `data-full` carries the absolute path for the clipboard handler and `/api/reveal` keeps using it unchanged; `.frow` becomes a wrapping flex row whose path holds `min-width:min(24ch,100%)` and whose count+reveal are grouped into one `.meta` unit that drops to its own line only when the row cannot hold both; the configuration row no longer emits an empty note span, so nothing props the grid open. Wording: the bulk full-text trigger loses its all-caps and exclamation mark for `⚡️ Enable full-text search for every dictionary…` (keeping the term "full-text search", which the mode dropdown, per-dictionary switch and result summaries all use), the tooltip is de-shouted, headings shorten to `LIBRARY`/`CONFIGURATION`, `no dictionaries found` → `no dictionaries`, `42 prepared, in use` → `42 prepared · in use`, and `#folders .facts .act` gives the three peer actions one appearance (`Edit folders…` was louder purely for being an `<a>`). Helpers verified in a node harness over 13 cases: the five real panel paths shorten correctly, `/Users/bio` alone becomes `~`, **`/Users/bioinformatics/x` is left intact** (the prefix test includes the separator), non-home paths are untouched, Windows `C:\Users\bio\…` shortens with backslashes, an empty `home` falls back to full paths, `&`/`<` escape before the `<wbr>` pass, and stripping `<wbr>` returns the exact absolute path. `folders_test.go` pins that `/api/config` reports `home`. `node --check` on the page script, `make check` green (15 packages, cgo), `make test-purego` green.

### P52 — Provenance rows carry their path — 2026-08-05 — COMPLETE (uncommitted)
D49, extending D48 to the dictionary cards. `provenance()`'s `label()` becomes `label(p,{pre,post,cls})`, emitting `<div class="prow">` with the path in its own `<span class="path" data-full="…">` and the annotations as sibling `.pfx` spans; new `note(text)` for the "contains: text.db + …" row, which names no path and therefore no longer wears `.path` or a "Click to copy" tooltip. The `#panelList` click handler drops `.replace(/^[a-z]+: /,"").replace(/ \+ media\.db$/,"")` and reads `dataset.full`. Paths now render through the D48 `tildePath`/`pathHTML` helpers, so cards show `~/…` with separator break opportunities; `word-break:break-all` on `.pd .path`/`.pd .prov` becomes `overflow-wrap:break-word`, `.pd .prow`/`.pd .pfx` are new and the dead `.pd .path.sub` rule is gone. New `escAttr` (esc + `"`) used for `data-full` and the two `title` attributes. `loadConfig()` split out of `loadFolders()` and run at boot via `Promise.all([loadPrefs(),loadConfig()])`, so `cfgInfo.home` exists before the first card is drawn; the `folders` toggle guard now asks whether the section has been drawn rather than whether config has loaded. Verified in a node harness over six card shapes (mdx+mdd+prepared folder, mdx+textDB, prepared-only, media-db-only, preview-only, and a path containing `"`, `&`, `<`): every `.path` shows the `~` form and copies the exact absolute path, the "contains" row is not copyable, and the quote is escaped in the attribute but not doubled in the value. The same harness confirms the two pre-existing defects this replaces — the old handler copied `/Users/bio/.wudict/db/AHD5 (text.db + media.db + info.txt)` for the database row and `text.db + media.db` for the "contains" row. `node --check` on the page script, `make check` green (15 packages, cgo), `make test-purego` green.

### P53 — An ETag for the page — 2026-08-05 — COMPLETE (uncommitted)
D50. `Server.indexETag` added beside `indexPage` and built in the same `sync.Once` (`page()`/`pageETag()` now both delegate to `buildPage()`), hashing the page **after** `{{VERSION}}`/`{{FRAMEJS}}`/`{{MARKJS}}` substitution via the existing `assetTag`. `handleIndex` sets the `ETag` and hands off to `http.ServeContent` with a zero modtime — chosen over a hand-rolled `If-None-Match` comparison because the header is a list, may be `*`, and GETs compare weakly (`W/"…"`), all of which net/http already implements; the zero modtime deliberately suppresses `Last-Modified`, a validator embedded bytes cannot support. `Cache-Control: no-cache` is untouched, so D45's freshness guarantee stands: the browser still asks on every load, it just no longer receives 102,574 bytes when the answer is "unchanged". The first-run setup branch gets no validator — its body is a function of unversioned registry state. New `TestIndexRevalidatesWithoutResending` pins the 200-then-304 round trip, an empty 304 body, `Cache-Control` surviving on the 304, weak (`W/"…"`) and list (`"deadbeef", "…"`) forms of `If-None-Match` both matching, an unknown validator still getting the page, the ETag changing with the version stamp, and no `Last-Modified`. Minification and gzip were measured and rejected in the same investigation (see D50). `make check` green (15 packages, cgo), `make test-purego` green.

### P54 — Fragment and external links in iframe articles — 2026-08-05 — COMPLETE (uncommitted)
D51. `frame.js` gains two branches before the bare-word one: a fragment link (`#…`) is `preventDefault`ed and turned into `post()` + `jumpToFragment(frag)` inside a `setTimeout(…,0)`, so the offset is measured after the dictionary's own handler has run — propagation is left alone so Cambridge's jQuery tabs still fire; an `http(s)://` link is `preventDefault`ed and posted to the parent as `{t:"open",url}`. `index.html` handles that message with `window.open(url,"_blank","noopener,noreferrer")` behind an `^https?://` test (a `javascript:` URL opened from the parent would run in the top page's origin), and gains the matching external-link branch for shadow-DOM articles, scoped by `dd` so the panel's own links stay ordinary navigation. `#` stays in the bare-word exclusion regex — it now has its own branch, and falling through would search `#dataset-british` as a headword — and the two stale comments claiming iframes handle fragments natively are corrected on both sides. Scope measured before fixing, by looking up `water` across the corpus for articles that are both script-bearing and carry fragment links: Oxford English Dictionary (37 in the collapsed article, **186 distinct fragment hrefs** in full), LDOCE6, LAAD3, OALD10 (4 each) and Cambridge (4, including both tabs) — broken since the iframe renderer existed. Verified by lifting both renderers' branch conditions into a node harness and dispatching every distinct href of the real Cambridge (74) and OED (517) articles through both: **591 hrefs, zero divergence** between the renderers, Cambridge classified as 4 fragment / 3 external / 3 audio / 64 bare-word, and links outside an article (`dd=false`) still falling to browser default. `node --check` on both scripts, `make check` green (15 packages, cgo), `make test-purego` green.

### P55 — Android app packaging — 2026-08-05 — COMPLETE (uncommitted)
D52 (nine-point matrix, user-approved point by point). `main.go`/`go.mod`/`go.sum` restored from HEAD — the gomobile experiment had left `x/mobile` imported, breaking every `CGO_ENABLED=0` build — and `build-android.sh` deleted. New `android/` module: dependency-free Java WebView shell (`MainActivity` + `ServerProcess` execs the binary from `nativeLibraryDir`; `HOME`/`TMPDIR` pointed at app storage; shared `Dictionaries` folder + app-private fallback; all-files-access dialog; singleTask + configChanges; back → WebView history), AGP 9.3.1, committed Gradle wrapper 9.6.1, minSdk 26 / compileSdk+targetSdk 36, vector adaptive icon traced from `favicon.svg`. Makefile: `android-go` / `apk` / `apk-release` (+ macOS JDK-17 fallback for Gradle). CI: `build-android.yml` — arm64 purego lib → release APK zipaligned + signed when the `ANDROID_KEYSTORE_BASE64`/`ANDROID_KEYSTORE_PASSWORD` secrets exist (debug APK otherwise), tagged runs attach the APK to the GitHub release. Verified locally: `make check` green (16 packages, cgo); `make apk` green; aapt2 badging and merged manifest correct; the shipped `libwudict.so` is PIE with 64 KiB segment alignment (16 KB mandate satisfied). Not done here by standing rule: on-device/emulator run — first manual install (`make apk` → `adb install`) is the user's smoke test.

### P56 — Android: cgo engine (FTS5 + speex parity) — 2026-08-05 — COMPLETE (uncommitted)
D53, upgrading D52's engine. `make android-go` now builds the cgo flavour with the NDK (resolution: `$ANDROID_NDK_HOME` → `$ANDROID_NDK` → newest in `$ANDROID_HOME/ndk`, darwin/linux prebuilt host dirs), `ANDROID_API=26` = minSdk, `-Wl,-z,max-page-size=16384`; `make android-go-purego` keeps the NDK-less fallback. CI installs `ndk;30.0.15729638` and exports `ANDROID_NDK_HOME`. No Go changes — the cgo file gates are `cgo`-only, so android inherits both (`driver_cgo.go`, `backend_cgo.go`). Verified locally: 11 MB lib, ET_DYN, `PT_LOAD` 0x4000 (16 KB mandate met under external linking), `DT_NEEDED` only public NDK libs; `make apk` green with the cgo lib inside; workflow YAML re-validated. On-device `.spx`/FTS5 smoke test remains the user's, per the standing rule.

### P57 — Android shell review: insets, back, external links, folders — 2026-08-06 — COMPLETE (uncommitted)
D54, reviewing D52/D53. `values-v35/styles.xml` deleted (its edge-to-edge opt-out is disabled for `targetSdk 36`); `MainActivity` pads the root by system-bar + cutout + IME insets, syncs system-bar icon appearance, and paints window/root/WebView from one `window_bg` colour with a `values-night` variant; `adjustResize` declared. Predictive back handled via `OnBackInvokedCallback` registered only while `canGoBack()` (`doUpdateVisitedHistory` keeps it in step), `onBackPressed` retained for API 26–32. `setSupportMultipleWindows(true)` + `onCreateWindow` (transport-WebView URL capture) + `shouldOverrideUrlLoading` send non-`ORIGIN` navigations to `ACTION_VIEW`, so D51's external links work on-device. `ServerProcess`: `seedConfig` writes `DICT_DIR` into `~/.wudict/wudict.toml` on first launch instead of passing `--dict-dir` (keeps the folders editable from the ☰ panel); `adoptRunningServer` adopts an orphaned child that still holds 6888, verified through `/api/config`; `awaitPort` watches the child and reports its last output line. `packaging` DSL rename, `waitFor()` in the version closure, `if` instead of `[ … ] &&` in the CI version step. Verified: `assembleDebug` and `lintVitalRelease` green (lint clean at minSdk 26 with every API-30/33 call version-guarded), merged manifest still `extractNativeLibs="true"`. No Go code touched, so D52's standing rule holds. On-device run remains the user's smoke test.

### P58 — Narrow-viewport bar: the search pill — 2026-08-06 — COMPLETE (uncommitted)
D55. `#q`/`#mode`/`#dict` wrapped in `.pill` with two `.seg` chips; `display:contents` above 600px keeps the desktop bar byte-identical in rendering, and the whole mobile treatment lives in the single `max-width:600px` block. Chips render `<select>`s that stay real and transparent over them (native picker, keyboard, `aria-label`, `.value` — no call site touched); `.seg` stretches to a 44px target. Inline SVG book glyph for the dictionary, following the panel's `.cog` precedent. `syncChips()` mirrors both selects, called from the two `change` handlers, `refreshDictUI`, `applyURL`, the `popstate` handler and once at boot; no option selected counts as the default, since a saved id is assigned before `/api/dicts` has created its option. `🔍`, `⇔`, `⊞` hidden below 600px; `◐` gains a second mount point in the panel head (`.headacts`), both driven by one handler over `.themeBtn`. `#mode` gains a `change` handler that re-runs the search, closing the hole left by removing the submit button. Gutters: `.col`, `dl`, `.article`, `summary`, `.scopenote` padding-inline `sp-2 → sp-1`; `body{padding-top}` → `calc(var(--barh) + var(--sp-2))`, deleting the `5.6em` constant. Verified: `node --check` on the extracted inline script; a 6-state harness over `syncChips` (both defaults, all three non-default modes, a scoped dictionary, and the pre-`/api/dicts` window where `.value` names an option that does not exist yet) — all correct; CSS braces balanced 177/177 and bar markup tags paired; `make check` green (15 packages, cgo). Visual check on a real phone and at 320/360/600px remains the user's.

### P59 — Search progress moves into the field; the status line collapses — 2026-08-06 — COMPLETE (uncommitted)
D56. New `.qbox` wrapper around `#q` (a real element at both widths — `.pill` is `display:contents` above 600px and cannot position an absolute child) holding `.qled`: an inline SVG `⌕` and a 14px `.spin` sharing one box, crossfaded by `.qbox.busy`. Field geometry (`flex:1;min-width:130px;max-width:300px`) moved from the input to `.qbox`; input gains `padding-left:2em` (`1.55em` inside the pill, where `.qled` sits at `left:0`). `setBusy(on)` with `BUSY_DELAY=200` / `BUSY_MIN=400`, called from `doSearch` (`setBusy(true)` in place of the old `Searching…` status), the all-disabled early return, and a `finally` guarded by `seq===searchSeq` so an aborted search cannot blink off the spinner of the search that superseded it. `#status` is now `display:grid;grid-template-rows:0fr` → `1fr` on `.show`, with `.msg` clipping and `.txt` carrying the padding inside the clip; `setStatus` writes into `#statusMsg`. Deleted `min-height:1.4em` (off the φ ladder and redundant against `line-height:var(--fi)`) and the outer padding. `prefers-reduced-motion` stops the rotation and holds the ring. Verified: `node --check` on the extracted script; a 14-assertion virtual-clock harness over `setBusy` covering the fast search that must never spin, the delay boundary at 199/201ms, the min-display hold, a finish past the min that clears at once, a re-search mid-flight that must not blink, and a cancel inside the delay window — all correct; static markup tag-balanced and CSS braces 189/189; `make check` green (15 packages, cgo). Visual check at 320/360/600/1280px and on the phone remains the user's.

### P60 — The 🔍 submit button removed — 2026-08-06 — COMPLETE (uncommitted)
D57, superseding D55's narrow-only hide. `<button type="submit">🔍</button>` deleted from the form and dropped from the responsive hide list; the surrounding comments now record why Enter still works (the no-submit-button clause of implicit submission, one blocking field) rather than why the button was hidden. No JS touched — `$("frm").addEventListener("submit",doSearch)` is unchanged, and the input handler's live search was already the ordinary path. Verified: zero `type="submit"` elements and exactly one implicit-submission-blocking field remain; static markup tag-balanced (button 6/6); CSS braces 189/189; `node --check` clean; `make check` green (14 ok packages). The one thing a browser must confirm: Enter submitting with no button present — the clause changed, even though the outcome should not have.

### P61 — Motion ladder and a calmer field spinner — 2026-08-06 — COMPLETE (uncommitted)
D58. New `--t-1…--t-6` tokens (`.236 .382 .618 1 1.618 2.618` s) beside `--sp-*`. `.qled .spin` is now `inset:2px` (10px, φ⁻¹ of the glyph) with `border-width:1.5px`, `border-color:transparent`, `border-top-color:currentColor` (inheriting `--fg-faint` from `.qled`, the same colour as the `⌕`) and `animation-duration:var(--t-6)` — 137.5°/s, the golden angle per second, 3.7× slower than the shared `.spin`. Crossfade `.18s → var(--t-2)`. `BUSY_MIN` and `busyShownAt` deleted: the 382ms fade is the anti-strobe guarantee, so `setBusy` drops from 16 lines to 8 and clears the moment results land. `BUSY_DELAY` 200 → 236 (φ⁻³ s). Verified: 13-assertion virtual-clock harness rewritten for the new contract — the 150ms search that never spins, the delay boundary at 235/237ms, immediate clear with no hold, `setBusy(true)` twice as a no-op, a cancel inside the delay window that must not fire late, and three chained 300ms searches holding one steady spinner — all correct; `BUSY_MIN`/`busyShownAt` confirmed absent; CSS braces 189/189; `node --check` clean; `make check` green (14 ok packages). Existing `.16s`–`.35s` transitions deliberately not retrofitted onto the ladder.

**P61 correction — 2026-08-06.** The spinner shipped invisible: `.qled .spin{inset:2px;width:auto}` let the shared `.spin`'s later, equal-specificity `margin-right:.4em` into the width solve, resolving to 3.6px. Replaced with explicit `top/left:2px; right/bottom:auto; width/height:10px; margin:0`, and the arc moved from `currentColor` (`--fg-faint`, the ⌕'s colour) to `--fg-soft`, because a 1.5px stroke carries far less ink than the glyph's and matching luminance does not match presence. Verified this time by resolving the cascade mechanically rather than by inspection — a small script (`scratchpad/cascade.py`) parses the stylesheet, matches every base-layer rule against `span.spin` inside `.qbox.busy > .qled`, sorts by specificity then source order, and prints the winning declaration per property: width/height 10px, margin 0, top/left 2px, right/bottom auto, border-top-color `--fg-soft`, animation-duration `--t-6` (beating the `.7s` inside `.spin`'s `animation` shorthand), opacity 1 under `.busy`. `node --check` clean, CSS braces 189/189, `make check` green.

### P62 — Resource overrides and damaged-blob reporting — 2026-08-07 — COMPLETE (uncommitted)
D59. `handleResource` gains `serveOverride`: `<library folder>/res/<name>` shadows the bundled blob, `Cache-Control: no-cache`, served with `http.ServeContent` (so Last-Modified revalidation works while the user edits). Name is `path.Clean`ed on a rooted copy and bounded by `within`. `nulWatcher` wraps the copy already in flight and flags a NUL in a source-text type (`.js .mjs .css .html .htm .xhtml .json .xml .svg .txt`); `Server.nulSeen` keys `id\x00name` so the warning is stated once. Bytes are passed through unaltered. New tests: `TestDamagedTextResourceIsReportedButServedVerbatim` (verbatim bytes, reported once, clean `.js` and binary `.bin` not reported), `TestResourceOverrideFromLibraryFolder` (no folder → bundled; after preparing, override shadows an existing resource, supplies a new one, `no-cache`, absent resource still 404s), `TestResourceOverrideRejectsEscapes` (positive control plus six traversal vectors asserted on *content*, since `path.Clean` legitimately folds `../x` to `x` inside the directory). Mutation-verified: with both guards removed, four vectors leak and the test fails. `make check` green (14 packages, cgo), `make test-purego` green. Investigation artifacts in the session scratchpad: an independent Python slob reader, a corpus-wide corruption scan, and a jsdom harness that proves the Cambridge tabs work under an intact jQuery. Applied for the user: `~/.wudict/db/en-en-Cambridge-English-Dictionary-Online/res/jquery.js` (intact jQuery 3.3.1, taken from the Merriam-Webster slob already in the corpus).

**P62 addendum — 2026-08-07.** The feature had a decision record and a phase record but no *instructions*: nothing in README, the CLI help or SPEC told a user the folder existed, and a mechanism nobody can find is not a feature. Documented in three places, each for a different reader — README gains "Replacing a file a dictionary ships (`res/`)" with a worked Cambridge example, how to learn a resource's name from the network panel, and the warning text quoted verbatim; the `--help` output gains a LIBRARY FOLDER section covering the same ground for someone who never opens a browser; and SPEC 6b records the endpoint behaviour and points at both. The library-folder listing in README also now shows `res/` alongside `text.db`/`media.db`/`info.txt`. A follow-up question — *can `res/` supply a resource that is missing rather than broken?* — exposed that the answer was yes and had been tested since P62, but that every word written about it said "replace": README and the CLI help now lead with both cases, and a nested-path assertion (`res/js/entry.js`) was added, since articles reference `js/…`/`css/…` and flat-only coverage proved nothing about them. The `.spx` asymmetry is recorded in D59 and documented for users rather than fixed.

### P63 — Iframe article height: ratchet and 6px runaway fixed — 2026-08-07 — COMPLETE (uncommitted)
D60. `frame.js` `post()` now measures `documentElement.getBoundingClientRect().height`, `body.getBoundingClientRect().height + 16`, `body.scrollHeight + 16`, and `documentElement.scrollHeight` only when it exceeds `documentElement.clientHeight` (real overflow, not the root's max(area, viewport) clamp); result `Math.ceil`ed. Growth path unchanged; shrinking works, so a tab switch to shorter content reclaims its space, and the parent's `+6` is no longer re-absorbed on every call. Verified by a 10-assertion simulation of the frame/parent loop (`scratchpad/ratchet.js`) built from the CSSOM definition and index.html's `clamp(h+6,48,60000)`: old measure never settles and grows 6px per `post()` (18px per article at load from the three timed calls alone) and leaves 492px blank after a tall→short switch; new measure settles in one step at content+22, survives repeated switching, and honours the 48px floor. `node --check` clean, `make check` green (14 packages, cgo), `make test-purego` green. Needs the user's eye on a real page — jsdom has no layout engine, so the simulation reasons from the spec rather than from a rendered frame.

### P64 — `format=` and the article sanitiser — 2026-08-08 — COMPLETE (uncommitted)
D61. New `internal/htmlref/sanitize.go`: `Sanitize(doc, Policy)` and `Text(doc)` over the same WHATWG tokenizer `Rewrite` uses, with `Policy{Tag, Attr, Bare, Replace}` — `TagKeep`/`TagUnwrap`/`TagDrop`, drop tracking by depth so a nested same-name element cannot end a drop early, an unwrapped-tag stack scanned from the nearest match so mis-nested markup leaks no stray end tag, and attribute values re-escaped on output. New `internal/server/articleformat.go`: wudict's policy (keep/drop sets, per-element attribute allowlist, `cleanURL` rejecting `javascript:`/`vbscript:`/non-image `data:`/protocol-relative and absolutising root-absolute `/res/…` against `originOf(r)`), plus `audioObject` rewriting DSL's `<object type="audio/*">` to `<audio>`. `handleSearch` parses `format`, 400s on an unknown value, and applies it after `RewriteEntryHTML` (order matters: reduce first and the references would not yet have been named). Tests: 18 sanitiser cases, 8 evasion shapes (case-varied `<SCRIPT>`, `ONCLICK`, unquoted handler, nested script), 8 `Text` cases, plus endpoint tests for raw/clean/text/400 and for the DSL pronunciation surviving as `<audio>`. Verified on the real corpus by running fetched raw bodies through the new reduction: 748 KB → 396 KB `clean` (1.9x) → 290 KB `text` (2.6x). `make check` green (14 packages, cgo), `make test-purego` green. Docs: `docs/CLIENT-API.md` §3 and §8 now describe `format` as implemented with the measured table; SPEC 6b pointer added.

**P64 addendum — 2026-08-08.** First round trip of the D55-kickoff feedback loop: the extension session read `docs/CLIENT-API.md`, found four contract gaps, and reported them instead of inferring answers from the server source — which is exactly the behaviour the Stage 1 prompt was written to produce. All four verified against the running server and fixed in the document, not in the client. **Error shape:** there is no `{"t":"error"}` frame; a per-dictionary failure rides on that slot's own `hit` frame, and a pre-stream failure is an ordinary HTTP 4xx with an `application/json` `{"error":…}` body — two different shapes needing two different checks, where the doc had implied one. **`skipped` shape:** also a `hit` frame, with `i`/`dict`/`name` and no `results`. Documented as a three-value `t` taxonomy with `hit` as the slot terminator carrying `results` **or** `skipped` **or** `error` **or** nothing (no matches). **A wart the extension could not have guessed:** `streamMsg.I` has no `omitempty`, so `begin` and `end` both carry a meaningless `"i":0` — their proposed "treat `i` as present ⇒ meaningful" rule would have misfired; the doc now says switch on `t` first. **Also:** `n` defaults to 20; Content-Type is `application/x-ndjson; charset=utf-8`; the `dict` list has no cap but the server opens only 8 dictionaries concurrently, so eight ids is the point of diminishing returns for a "more" action; unknown ids are silently dropped and only an all-unknown list 404s; empty `q` is a 400; and `begin.slots[].name` is the id repeated, not the dictionary name (naming requires an open, and `begin` precedes every open). Frame examples in §3 are now captured verbatim from a live server rather than written by hand.

**P64 addendum 2 — 2026-08-08.** `docs/CLIENT-API.md` gains §9 (opening the full entry in the wudict page) and §10 (audio), both verified against the source rather than described from memory. §9 documents `/?q=&mode=&dict=` as a supported link target — the page's query string, not its HTML — and records two traps found while checking `applyURL`: it assigns only parameters that are **present**, so a "search everywhere" link that omits `dict` silently searches whatever single dictionary the user last selected (a client must always send `mode` and `dict` explicitly); and `dict` is assigned to a `<select>`, so it takes exactly one id — the comma list of §3 is an `/api/search` feature and would match no option here. Also recorded: an explicit `dict=<id>` bypasses the enabled set, so a link to a disabled dictionary still opens it, and `applyURL` runs after `/api/dicts` completes so a valid id is always in the dropdown by then. §10 scopes audio to anchors matching `/\.(mp3|ogg|wav|spx|m4a)(\?|#|$)/i` and reproduces `playAudio` verbatim, with the reason the single reused `Audio` element is load-bearing (`.spx` is transcoded server-side, so an unreferenced `new Audio(url)` can be collected mid-load), that playback decodes by Content-Type not extension (`.spx` is served as `audio/wav`), that `closest("a[href]")` is needed because dictionaries wrap a speaker image, that `preventDefault` stops the popup navigating to a bare media player, and that an MV3 service worker has no `Audio` constructor. Notes that `format=clean` yields two audio shapes — native `<audio>` from the DSL rewrite plus these anchors — so a client needs both paths.

**P64 addendum 3 — 2026-08-08.** `docs/CLIENT-API.md` gains §11, the cross-reference grammar inside article bodies, verified against `parseRef` and both click handlers rather than described from memory: the scheme test `/^(?:(?:bword|entry):(?:\/\/)?|[dx]:)/i` (six spellings of one idea), the split at the **first literal `#`** before percent-decoding (a `#` inside a headword arrives encoded, so decoding first would promote it to a delimiter), the three kinds `parseRef` yields — `lookup`, `sub` (`@`-prefixed, an MDict repack's collapsible section stored as a headword) and `anchor` (scheme with only a fragment) — and the bare-href test `!/^([a-z][\w+.-]*:|\/|#|res\/|assets\/)/i`, with the note that dropping any exclusion means searching `#sense2` as a word. States the rule the user asked to be unambiguous: **a click that changes which word or which dictionary is displayed leaves the popup for a full wudict tab**, because a popup has no address bar, no back button and no room. Records that a `lookup` must be scoped to the `hit` frame's `dict` — the dictionary the author wrote the link in, not the user's current selection — and that `&from=<id>` is the app's own equivalent. Flags the one shape where a tab is the wrong answer: an `anchor` points inside the content already on screen, `format=clean` preserves `id` attributes so the popup should scroll to it, and `applyURL` does not read `location.hash` so a tab could not restore the position anyway. Also records that every link needs `preventDefault` first, because in a popup injected into a third-party page a bare `<a href="defendant">` resolves against the **host page's** URL — the §5 base-URL trap arriving through a different door.

### P65 — Zensical documentation site — 2026-08-12 — COMPLETE (uncommitted)
Created `pages/` — a full Zensical 0.0.50 project (modern `.toml` config, overrides, extra CSS, logo/favicon from the project's own SVG). Ten content pages: home (card grids, buttons, format table), about (wu-wei etymology + dual-engine), install (per-OS tabs, two-flavour tip, build-from-source), run (mermaid flow, config table, CLI), use (search modes, shortcuts, streaming, panel), extension (Chrome/Firefox store buttons → FIXME.TODO.com, manual install, options, privacy, status badge), dictionaries (format table, library layout, res/ overrides, damaged-file warning), service (macOS launchd, Linux systemd, Windows, Android), FAQ (11 collapsible Q&A), license (GPL-3.0 + bundled attribution table). Widgets: grids, tabs, admonitions, buttons, code annotations, mermaid, keys, tables, tooltips, tags, status, collapsible details. `zensical build` clean — zero issues.

### P66 — Play flavour: SAF import, no storage permission — 2026-08-13 — COMPLETE (uncommitted)
Split the Android app into `foss`/`play` product flavours on a `dist` dimension (D62), Go and web assets untouched. `src/main`'s manifest now declares only `INTERNET`; `src/foss` re-adds `MANAGE_EXTERNAL_STORAGE` + the two `maxSdkVersion`-capped legacy lines + `requestLegacyExternalStorage`; `src/play` adds no permission and gains `ACTION_SEND`/`SEND_MULTIPLE` filters. One flavour-private `Storage` class is the whole seam (`dictDirs`/`ensureAccess`/`handleShellUri`/`onActivityResult`/`onPageFinished`/`onNewIntent`); `MainActivity` lost `ensureStorageAccess` and gained `reloadPage`/`onNewIntent`/`onActivityResult` delegations, `ServerProcess` now takes its dirs from `AppDirs`+`Storage`. New `AppDirs` puts HOME, `wudict.toml` and `db/` in `getExternalFilesDir()` for **both** flavours (user-directed — the private dir is unreachable on unrooted phones), preserving a legacy internal root when `filesDir/db` already has content and copying the TOML across otherwise. `src/play/SafImporter.java` (~380 lines, dependency-free) mirrors a picked `ACTION_OPEN_DOCUMENT_TREE` subtree into `files/Dictionaries` — persistable grant, breadth-first enumeration with depth/entry caps, `StatFs` precheck, `.part`+rename, name sanitising with canonical containment, cancellable with progress, then `/api/rescan` + page reload, then an optional `DocumentsContract.deleteDocument` pass that turns the import into a move. Strings split per flavour. Makefile: `apk`/`apk-release` → foss, new `apk-play`, `aab-play`, `apk-verify`; new `tools/verify-apk.sh` asserts the permission split and `extractNativeLibs=true` on both APKs, and CI now runs it. Export postponed indefinitely (user-directed). Verified statically: both debug APKs build, all four `verify-apk.sh` assertions pass, `bundlePlayRelease` produces the AAB at the Makefile's path with `uncompress_native_libraries` off (16K alignment preserved), `make check` green.

### P67 — Play flavour: SD-card volumes and the setup-page dead end — 2026-08-13 — COMPLETE (uncommitted)
Two follow-ups to P66, both from tracing what `/setup` does under SAF (D62 amendment). `AppDirs.dictRoots` replaces the single-folder `Storage.dictDirs`: `Dictionaries` under every mounted, writable `getExternalFilesDirs()` entry, deduplicated by canonical path, each created so it is visible over MTP — a microSD card is now scanned with nothing typed and no permission. `ensureAccess` gates the first-run prompt on `hasContent(File[])` over all of them, so a card filled over USB is not treated as an empty install; `seedConfig` gained a comment stating the first-launch-only consequence rather than teaching the shell to rewrite the user's TOML. `Storage.onPageFinished` now falls through to `setup.html` when the panel header is absent, inserting a worded "Import dictionaries…" button before `#save` — closing the dead end where tapping "Later" left the picker reachable only by relaunching. Both anchors are static markup, never the async-built folder rows. `tools/verify-apk.sh` fixed: it selected one APK per flavour by listing order and had been asserting against a stale `play/release` while ignoring the debug build just made — it now checks every APK present. Verified: both flavours build; all six assertions pass; the injected script `node --check`s clean and was simulated against stubbed setup-page and index-page DOMs (correct anchor, correct label, `wudict://import` on click, second run inserts nothing); `make check` green.

### P68 — Per-dictionary removal, offered where no file manager exists — 2026-08-13 — COMPLETE (uncommitted)
D63. New primitive at every layer, gated by one rule. **dict:** `companions.go` — `stem`/`mainExt`/`indexCompanions`/`SourceFiles`, with `CompanionMedia` moved verbatim out of `internal/server` (it already held half the sibling table; a second copy is the duplication CLAUDE.md forbids). Fixed while testing it: a compressed DSL's media zip is `x.dsl.files.zip` beside `x.dsl.dz`, a spelling the old candidate list missed. **store:** `remove.go` — `RemovePrepared` (parent must be the library root, `Lstat` so a symlink is refused not followed, the root itself refused) and `TreeSize`; the whole file-deleting surface, one guard. **server:** `remove.go` — `Registry.Remove(id, dropPrepared, dropSource)` under `ingestMu`, `closeNow()` (synchronous, unlike `evict`'s grace timer: Windows will not unlink an open file), `DELETE /api/library?dict=&prepared=&source=`, `removalOffered = !(isLoopback && revealPossible)`; `configInfo.CanDelete` beside `CanReveal`, which now *also* requires `revealPossible()`. `Rescan` was rebuilding the entry list but appending into the old `byID`, so a removed dictionary stayed addressable by id — fixed to rebuild the map. **cli:** `wudict rm [-f] [-keep-source|-keep-index] <name|path>`, dry run by default, resolving a library name, folder, `text.db` or an original file. **web:** a `.rm` control in each card's `.acts`, rendered only when `cfgInfo.canDelete`; the card turns into its own two-step confirmation (`delete everything` / `index only` / `original files only`, the last only when the media is packed) and `runRemove` reports the server's freed bytes and note. **android:** `ManageSpaceActivity` (translucent, `excludeFromRecents`, launches MainActivity with an extra and finishes) + `android:manageSpaceActivity` on `<application>`, replacing App Info's "Clear storage"; MainActivity clicks the page's own `#panelBtn` from `onPageFinished`. No Go build tags, no Java in `src/main` that knows a flavour.

Verified: 12 new Go tests — `SourceFiles` for MDX (numbered `.mdd` run stopping at the first gap, neighbouring stem untouched), StarDict (`res/`, index companions, unrelated suffix ignored), compressed DSL, single-file formats and a missing main file; `RemovePrepared` against six refusal shapes plus a symlink whose target must survive; `TreeSize`; and endpoint tests for the desktop 403 + `canDelete:false`, the no-file-manager offer, source-only refused twice (409 without `USE_CACHED`, then 400 naming the unpacked media) with the source still on disk after each, and a full removal that takes the `.dsl` and its `.files.zip`, leaves the neighbour, and drops the id from the registry. `folders_test` now states `revealPossible` rather than inheriting the build machine's. The panel's confirmation logic was simulated over five dictionary shapes (packed / unpacked / unprepared / no-source / no-media) — option sets all correct; `node --check` clean, CSS braces +8/+8. `make check` green (15 packages, cgo), `go build`+`go vet -tags purego` clean, both APK flavours build and all six `verify-apk.sh` assertions pass. On-device behaviour — App Info → "Manage space" opening the panel, and a real deletion freeing real space — remains the user's.

### P69 — Path display stops claiming `~` where no shell exists — 2026-08-13 — COMPLETE (uncommitted)
D63 amendment. `configInfo.Home` → `PathAliases [][2]string` (`omitempty`), built by `pathAliases(shown)` over the paths the panel actually renders (library dir, config file, every root). Desktop: one pair, `$HOME → ~` — identical output to D48. Android (`runtime.GOOS == "android"`, no build tag): `androidAliases` derives the Files app's own volume names from those paths — `/storage/emulated/0` and `/sdcard` → `Internal storage`, any other `/storage/<id>` → `SD card (<id>)` (`self` and a bare `emulated` skipped, they are not volumes), `/data/user/<n>/<pkg>` and `/data/data/<pkg>` → `App storage` — deduplicated and sorted longest-prefix-first. Page: `tildePath` → `shortPath`, applying opaque pairs at segment boundaries only, still display-only (`data-full`, clipboard and `/api/reveal` keep the absolute path). Nothing else read `home`: `ServerProcess.adoptRunningServer` probes `libDir`/`revealLabel`, and `setup.html`/`frame.js` never used it.

Verified: `TestAndroidAliases` over four device shapes (Play — everything under the app's external files dir; FOSS — internal flash + shared folder; a removable card plus `/sdcard`; and a list of non-volumes and repeats yielding one pair) plus a partial-segment case proving `/storage/emulatedX` is not read as the primary volume; `folders_test` now asserts the `~` pair instead of `home`. `shortPath` simulated over 10 inputs (no aliases, exact match, prefix match, near-miss sibling `/Users/bioinfo`, Windows separators, both Android pairs, empty prefix and empty path) — all correct. `gofmt` clean, `go test ./internal/server` green, `go vet` for `GOOS=android -tags purego` clean, `node --check` clean.

### P70 — The Play import control becomes discoverable — 2026-08-14 — COMPLETE (uncommitted)
D62 amendment. `Storage.IMPORT_BUTTON_JS`'s index branch now anchors on `#panel .panel-head` and inserts a full-width worded `📥 Import dictionaries…` button before `.allrow` (appending to the head if that row is ever absent), replacing the `↥` icon in `.headacts`. Inline styles use only the page's custom properties. Setup branch, id guard and `wudict://import` channel unchanged; no web asset, no Go code, no build tag.

Verified: the Java string literal was reassembled and `node --check`ed, then run against a stub DOM of the real panel markup — the button lands in `.panel-head` between `<h2>` and `.allrow`, carries the worded text, is `width:100%`, navigates to `wudict://import` on click, a second injection adds nothing, and the `/setup` branch still inserts its button before `#save`. `make apk-play-debug` builds.

### P71 — Android power, memory and thread budget — 2026-08-14 — COMPLETE (uncommitted)
D64. The earlier performance rounds (PERF §6b/§6c) had only the desktop in view; this one asks what the *platform* will tolerate. Three tiers, no build tags, no Go code that exists only for the port.

**Power state.** New `internal/server/power.go`: `Power` (`active`/`background`/`restricted`) with `ParsePower`/`String`, `POST /api/power?state=` (loopback-only, 400 on an unknown state), `SetActiveProcs` (GOMAXPROCS while active, 1 while not), `SetMemoryLimit` (installs the ceiling *and* arms the shedding that makes it safe), `memoryPressure()` via `runtime/metrics` — never `ReadMemStats`, which stops the world — at 0.85 of the limit, and `Registry.SetPower`/`shed`. `Warm` and `maybeAutoIndex` now refuse outside `PowerActive`, the auto-index refusal deferring without consuming `autoTried`. `sweep()` gained the pressure rules (target 0, idle grace waived) and the janitor became event-driven: `needsSweep()` + a `wake` channel instead of a 20 s tick forever. The last direct `debug.FreeOSMemory()` (in `entry.reopen`) became `scheduleReclaim()`. Java: new `PowerSignal` (coalescing, 3×800 ms retry, thread exits after 30 s idle), `MainActivity` gained `onStart`/`onStop`/`onTrimMemory`, a thermal listener (API 29+, unregistered first in `onDestroy`) and `applyPower()` — hot → restricted, invisible → background unless charging, power-save → background.

**Defaults.** New `internal/config/tuning.go`: `MaxProcs` (half the cores, 2–4, Android only), `previewMemoryDefault` (64 MB vs 1 GB), `memoryLimitDefault` (RAM/16 floored 192 MB capped 384 MB on Android, unset elsewhere, reading `/proc/meminfo`). `internal/store/compress.go` writes at level 1 on Android; `internal/cli/cli.go` wires `MaxProcs` into `server.SetActiveProcs` + `search.SetWorkers` and routes `MemoryLimit` through `server.SetMemoryLimit`.

**Not doing unnecessary work.** `internal/store/pool.go` — one idle connection, 30 s idle TTL, 1 MiB page cache on Android, and *not* `SetMaxOpenConns` (a nested query on one handle would deadlock rather than queue). `Store.mediaDB()` opens `media.db` on first resource use and remembers a failed or foreign one.

**Measured, not asserted — and the measurement changed the design.** `internal/server/memlimit_test.go` — env-gated (`WUDICT_PERF_CORPUS`), one fresh child process per limit, `runtime/metrics` throughout, rows streamed to stderr so a run interrupted at minute 40 does not throw away what it measured. Against the real 130-dictionary corpus in preview mode (2 h, seven children) **every ceiling from 96 MB to 512 MB failed identically**: GC pinned at 44–46 % of CPU against a 16.6 % baseline, wall 7.4–8.8× the 140 s baseline, no trend in the limit and no knee — the runtime's own 50 % GC limiter is what holds the number down. The pressure path is not at fault and is not sufficient: it really does shed (106 open backends → 0–1, peak RSS 7.2 GB → 5.3 GB) but at 512 MB in-use was still 3.8 GB, because what remains is the fan-out's own working set. Two consequences. **(a)** The ceiling was made corrigible rather than better-guessed: `adjustLimit` at the end of every janitor pass, raising the limit to the footprint + 25 % after 3 consecutive pressured passes and restoring the configured value once the footprint is clear of it, with `needsSweep` no longer returning false in the one state that needs it. One unprepared dictionary in this corpus holds a 395 MB index — larger than the whole ceiling a phone gets — so the pathological state is reachable on a phone, not only on this desk. **(b)** A standing **desktop** finding, recorded and not fixed here: `PREVIEW_MEMORY` is not enforced during a `dict=all` fan-out — 64 MB configured, 6.3 GB held — because `minEvictIdle` (45 s) protects every backend a fan-out keeps touching, so the budget only binds between bursts.

**On device, where one line of the runtime undid the rest.** Verified on `emulator-5554` (API 36 arm64): `GOMAXPROCS=2, search fan-out=2`, `memory limit: 247 MB`, `PowerSignal` retrying past the startup `ConnectException`, active↔background across HOME, and `TRIM_MEMORY_RUNNING_CRITICAL` → restricted with the 30 s recovery firing at +30.006 s. Then the memory: one 241 MB MDX (464,360 headwords, `AUTO_INDEX = "off"`) took `VmRSS` 16.7 → 119.5 MB on a search, and backgrounding logged the eviction while RSS stayed at 184 MB — unchanged over three cycles and unmoved by `FreeOSMemory`. Not a retention bug: the same `open → Prefix → evict → reclaim` path on the desktop takes the heap 93.9 MB → 0.7 MB. The runtime defaults `madvdontneed = 1` only when `GOOS == "linux"` (`runtime1.go:parseRuntimeDebugVars`), so an `android` build keeps `MADV_FREE` and freed pages stay charged until the kernel needs them — invisible releases, to the one audience (LMK, vendor watchdogs, battery screen) the whole tier exists to satisfy. `ServerProcess.java` now exports `GODEBUG=madvdontneed=1` to the child; the same run then reads 119.5 → **19.9 MB**. Two measurement corrections belong with it: Android toybox `ps -o RSS` reports 4 KB pages, not KB — an early reading of "29 MB" was 116 MB, and `/proc/<pid>/status` is the only source used now; and the `previewBytesPerEntry = 350` model over-charges a warm MDX (measured 83 B/entry at open, 201 B/entry once the folded index is built), which is the right direction to be wrong in and is kept, now labelled an estimate.

**On real hardware (OnePlus PKG110, Android 16, 8 cores, 11.5 GB, 61 unprepared dictionaries).** Derived defaults confirmed: `GOMAXPROCS=4, search fan-out=4`, memory limit 384 MB (capped from 718 MB), preview budget 64 MB. Eight `dict=all` prefix queries: 62 s wall, 130 s CPU, VmRSS 288 MB → **4.9 GB**, the ceiling relaxed five times (384 MB → 6312 MB), and the platform's thermal status put the app into `restricted` 25 s in — from heat this program produced, which is the best evidence in the record for halving `MaxProcs`. It survived only because the phone has 11.5 GB; PERF §8.2's fan-out defect is therefore an Android problem too, and is still unfixed. It also exposed a defect in the relax valve itself: `restoreMemoryLimit` runs only inside a janitor pass, and after a relax the janitor blocks (nothing reclaimable, and no pressure *because the ceiling is now 6 GB*) — so one heavy search bought a 6 GB ceiling for the life of the process. `needsSweep()` now stays true while `limitRelaxed()`, and the restore arms a `scheduleReclaim`. Re-measured on the same device: 4032 MB → 173 MB within 60 s of backgrounding, `memory limit restored to 384 MB` one janitor period later, 10 ms of CPU over the following three minutes. PERF §8.4–§8.5.

**On the low-RAM device (Xiaomi MI PAD 4, Android 11 / SDK 30, 6 cores, 3.8 GB, 24 unprepared dictionaries).** Derived `GOMAXPROCS=3, search fan-out=3`, memory limit 233 MB. Four `dict=all` queries: 177 s wall, 355 s CPU, VmRSS 7.7 MB → **1.71 GB** (45 % of the machine), MemAvailable 2.2 → 0.57 GB, ten ceiling raises, 63 evictions; then idle recovery to 110 MB with `memory limit restored to 233 MB` and flat CPU — §8.5's fix holding on a second Android generation. Recovery is slow by construction (`minEvictIdle` 45 s + 20 s janitor period + reclaim-time `MADV_DONTNEED` ≈ a 4-minute tail).

**Third defect, found there: abandoned searches ran to completion.** The UI aborts its fetch on every keystroke (`searchAC`), and three fetches abandoned at 4 s still cost 90 s each and 1.0 GB — `search.StreamOpen` consulted the context only while a worker waited for a semaphore slot, so with a free slot the `select` had two ready cases and half a cancelled queue opened dictionaries for a client that had hung up. It now re-checks `ctx.Err()` after the slot and after the open; measured on device, an abort at 1 s opened 14 of 24 instead of all 24. Residual, deliberately left to the §8.2 decision: an open in flight is uninterruptible, and one 2.88M-headword MDX is 6–11 s and hundreds of megabytes by itself, so that same aborted request still ran 9.9 s. Cancellation bounds a dead fan-out to the opens already running; bounding the rest needs the per-fan-out materialisation cap. PERF §8.6–§8.7.

New tests: `search.TestStreamOpenCancelledBeforeStartOpensNothing` (200 openers under a pre-cancelled context must open none — the random `select` alone would open about half), `power_test.go` (state parsing round-trip, parallelism including "an unmanaged platform is left alone", shedding on background and the dictionary still answering afterwards, the deferred auto-index completing once active, the janitor idling with nothing to reclaim *except* under an impossible ceiling, pressure tracking the limit, the relax taking effect only on the third pressured pass and not creeping afterwards, the restore's both boundaries, the janitor refusing to sleep while the ceiling it raised is still raised, and the endpoint's 200/400/403), `config/tuning_test.go` (both platforms' branches and that `defaults()` uses them), `search.TestWorkers`, `store.TestMediaOpensLazily` and `TestForeignMediaRejectedOnce`. Two of the new tests failed on first run and both were test bugs, fixed: `httptest.NewRequest` defaults `RemoteAddr` to 192.0.2.1, so the accepted `/api/power` calls were being 403'd as remote; and the restore test constructed a configured ceiling *above* the one in force, which is not a relaxed state at all, so there was nothing to restore. Verified: `make check` green (cgo, 15 packages), `go build -tags purego ./...` and `go vet` for `GOOS=android GOARCH=arm64 -tags purego` clean, power tests green at `-count=3` (the knobs are process-global; a leak between tests would show there).

### P72 — The per-search materialisation cap — 2026-08-14 — COMPLETE (uncommitted)
D65, closing PERF §8.2 and the residual P71 left open. `internal/server/registry.go`: `entry.lastWeight` (the price of an open, remembered across eviction — `weight` cannot serve, it must go to zero when the backend is dropped because the sweep totals it), `fanout` (one `atomic.Int64` per query; `admit` is a CAS loop that declines an oversized entry without consuming the remainder, `settle` corrects the reservation to the measured weight and returns it in full on a failed open), `tooHeavy` (a refusal that names the megabytes and the remedy), `entry.openWithin` (already-open → free; priced and too big → refused before any bytes exist; unpriced → admitted then charged; **prepared → never refused**, via one `preparedFor` stat on the refusal path only). `handleSearch` mints one budget per request and passes it to every opener; a declined dictionary keeps its slot in the stream with `Error` set, so the page shows "not searched" rather than an empty result. Config: `SEARCH_MEMORY` (`config.SearchMemory`, env + TOML + sample block), `searchMemoryDefault()` in `tuning.go` — **0 off Android**, `memoryLimitDefault()` on it, the reasoning being that letting one query create more than the ceiling guarantees the relax valve fires. CLI: `SetSearchBudget` wiring, the startup line's `· N MB per search`, a `-v` line and a help block.

Verified: five new tests in `internal/server/fanout_test.go` — the budget packs rather than stops (a refusal must not spend the remainder; overshoot may go negative; a failed open returns its reservation), a nil fan-out admits everything without allocating, a real registry opens 1 of 4 and refuses 3 under a one-byte budget with an already-open backend never refused and a priced-then-evicted one refused with the remembered size and zero bytes materialised, an HTTP search under a one-dictionary budget answering for all four slots with some declined and none declined once uncapped, and a self-preparing DSL admitted against a budget already blown by 1 MB. `config/tuning_test.go` extended: `searchMemoryDefault()` must be exactly 0 off Android, `0 < n <= memoryLimitDefault()` on it, and `defaults()` must use it. Two test bugs caught and fixed rather than papered over: the first version of the registry test expected a priced refusal from dictionaries that had never been opened (nothing was ever priced, so all were admitted as unknown), and the HTTP test assumed a cold library could be capped at all — a fan-out is parallel, so every opener reads "unknown" before any settles a charge, and the cap binds from the second search on. That is a real property, now stated in D65 and in the test's comment. One design bug caught before it shipped: with `est = 0` for a never-opened *prepared* dictionary, a spent budget would have refused something that weighs nothing and saves nothing when refused. Re-measured on the MI PAD 4 against an uncapped control run of the same binary minutes apart (PERF §8.8): four cold `dict=all` queries over 24 unprepared dictionaries went from 212 s / 438 s CPU / 1.30 GB peak to **61 s / 123 s / 1.02 GB**, latencies from 99·49·39·25 s to 56·1·1·1 s, and the corpus's 961 MB headword index — opened three times in four queries by the control — was declined by every query after the one that priced it. Two honest limits came out of the same run and are recorded in §8.8: the peak fell only 22 % because the budget is per query and a resident dictionary is free, so a burst inside the 45 s idle grace still stacks (the janitor bounds the residue, the cap does not); and the *first* query of a cold library cannot be capped at all — 56 s and 393 MB of the capped run is one query paying to learn 24 prices, which persisting `lastWeight` across restarts would fix. Idle recovery unaffected: 34.8 MB and the ceiling restored 20 s after the burst, 10 ms of CPU over the next 200 s.

### P73 — v3 MDD keys are UTF-8, not UTF-16 — 2026-08-14 — COMPLETE (uncommitted)
The three "corrupt or unsupported file" failures noted in PERF §8.8 were a wudict defect, not damaged files: `/opt/homebrew/bin/mdict -k` reads all three. Two facts had to be established before the code made sense. First, **the panic came from the companion `.mdd`, not the named `.mdx`** — the `.mdx` alone opens fine; copying the sibling `.mdd` next to it reproduces the panic under the `.mdx`'s name, which is why the first desktop attempt (only the `.mdx` files had been pulled from the device) passed. Second, the short-read hypothesis was **disproved by measurement** rather than assumed away: `dd` up to `bs=8M` and a 3000-probe reader comparing the device's FUSE-mounted file against an identical ext4 copy showed 0 short reads and 0 differing bytes.

The defect: `splitKeyBlock` derived its terminator width and its decoder from `fileType == MdictTypeMdd` instead of from `meta.encoding`, which `readDictHeader` already sets correctly — UTF-16 for v1/v2 MDD, UTF-8 for **everything** v3, MDD included. A v3 MDD key block scanned at a two-byte stride steps over every one-byte NUL, so the loop either read `keyBlock[i+1]` one past the end (`index out of range [143] with length 143` — the moliner `.mdd` is exactly 143 bytes decompressed, dumped and read by hand to confirm the layout: 8-byte BE offset, UTF-8 name, one NUL) or left `keyEndIndex` at the previous entry's value and sliced backwards (`slice bounds out of range [32734:32724]`). Fixed by reading `meta.encoding`, which is identical for v1/v2 and correct for v3, and which also removes a double decode (the old code ran the UTF-16 branch and then the MDD branch over the same bytes). Three robustness defects in the same loop went with it: the `numberWidth` slice is now bounded by the loop condition, "no terminator in this block" takes the remainder instead of reusing a stale end index, and the bare `panic(err)` on a failed UTF-16 decode is gone — the graceful raw-bytes fallback the branch above it already had now covers both. Nothing outside `splitKeyBlock` changed; `readKeyBlockInfo`'s identical `|| fileType == MdictTypeMdd` clauses are redundant but harmless, since only v1/v2 reach them and there the two conditions agree.

Verified against the real files rather than by inspection: all six (three `.mdx` + three `.mdd`, v3, 81554/26687/29303 headwords and 6/3785/3930 resources) now build an index, and every key set is **identical after sort to `mdict -k`**. Regression on five v1/v2 MDD files from the desktop corpus (54164/3930/1/3716/1 keys): also identical, unchanged by the fix. End to end with a cgo build: every resource in the moliner `.mdd` extracts to the right bytes (`file` reports JPEG/Apple Desktop Services Store), the wordreference `.mdd` yields a playable MP3, the britannica `.mdd` a JPEG, and `lookup caldero` renders the article. New `internal/gomdict/keyblock_test.go` — the first tests in that package — is table-driven over the four encoding × file-type combinations plus three malformed blocks (unterminated final key in each width, a tail shorter than a number), asserting keys and record offsets. `make check` green (15 packages, cgo), `go build -tags purego ./...` and `GOOS=android GOARCH=arm64 go vet ./internal/...` clean. FORMATS.md gains the rule (encoding, never file type; check the `.mdd` when an `.mdx` is called corrupt) and PERF §8.8's "not investigated" note is corrected.

### P74 — Deferred, not declined: a tap is the signal — 2026-08-16 — COMPLETE (uncommitted)
Amends D65. The cap shipped in P72 reported a refusal as the slot's `Error`, reading *"not searched: ~31 MB unprepared, over this search's memory budget — prepare it to search it for free"* — our vocabulary, naming a remedy the UI has never offered (preparation is silent and automatic by D20/D15; there is no "prepare" verb anywhere), and, the real defect, **a refusal that blocked its own remedy**: `handleSearch` fires `maybeAutoIndex` only after a *successful* open while `openWithin` refuses *before* opening, so a declined dictionary was never opened, never indexed, never prepared, and weighed exactly the same on the next query — and since admission runs in the user's preference order it was deterministically the same tail of the library, permanently. The cap was not deferring those dictionaries, it was deleting them.

The rule adopted, which needed no new limit because every part of it already existed: **automatic while it is cheap, on demand once it is not.** `internal/server/server.go`: `streamMsg` gains `Deferred bool` + `Bytes int64`; `errors.As(h.Err, &tooHeavy{})` sets them and leaves `Error` **empty**, so no technical string reaches the client; a search naming exactly one dictionary runs uncapped (`fan = nil` when `len(entries) == 1`) — the budget bounds *fan-out*, one dictionary has no fan-out, its cost is bounded by itself, and `PREVIEW_MEMORY`/the janitor/the relax valve still bound the steady state, whereas refusing it leaves the dictionary unreachable by every path the app has. `internal/server/registry.go`: `entry.demandIndex()` — same `ensureBaseIndex` work as `maybeAutoIndex` with one difference, the lane (`frontLimit`, "the user is waiting", instead of the FIFO `indexLimit` that is hours long on a hundred dictionaries), CAS-once via a new `demanded` flag because a search runs on every keystroke, and released again when power is not `Active` so the deferral is postponed rather than cancelled; `tooHeavy.Error()` reshaped as a log sentence, since it is now read only by logs and tests. `web/index.html`: a `h.deferred` branch in `renderSlot` painting a normal collapsed `details.dict` with a quiet `tap to search` / `click to search` hint (`matchMedia("(pointer:coarse)")`), no ⚠ and no megabytes — a size figure is not a decision the reader can make; a per-element `toggle` listener (`toggle` does not bubble, so delegation on `#out` would silently never fire) that re-queries `dict=<id>` into the same box, forces the section open, paints an explicit "no results" section when the answer is empty (`renderSlot` clears `box.innerHTML`, so otherwise the tapped section would vanish), and re-arms itself on failure or abort; `[data-deferred]` excluded from expand-all, which would otherwise become a dozen simultaneous opens; and a `hitName()` fallback preferring the name `/api/dicts` already knows over the stream's filename fallback, which is why the device screenshot read `Dorland 32th.mdx` — it improves every failed-open card, not only deferred ones. No "prepare" button was added: that is what D31 removed as "index all", a control named after our mechanism instead of the user's outcome.

Verified: `fanout_test.go`'s HTTP test rewritten to the new contract — under a one-dictionary budget every slot is still accounted for, a deferred slot carries `deferred`, a positive `bytes`, a name, and **no error string**, and the test then performs the tap itself, asserting that asking for the deferred dictionary alone answers with results and no deferral, and that an uncapped re-run declines nothing. Four design bugs caught by reasoning before they could ship, listed above where they belong (`toggle` not bubbling, expand-all mass-triggering, the cleared box, and an unguarded demand-index queueing an ingest per keystroke). `make check` green (15 packages, cgo), `go build -tags purego ./...` and `GOOS=android GOARCH=arm64 go vet ./internal/...` clean. Two `gofmt -l` hits (`internal/htmlref/htmlref_test.go`, `internal/store/ingest.go`) are pre-existing comment-alignment drift in files this phase did not touch.

### P75 — The import says what it did, to which files — 2026-08-16 — COMPLETE (uncommitted)
D66. `android/app/src/play/res/values/strings.xml`: `Copying %d of %d` → `Importing %d of %d`; the delete offer rewritten to name the picked folder in quotes and the bytes deleting frees, with a `_nosize` variant for providers that report no sizes and `import_source_fallback` ("the folder you picked") for providers that will not name the tree; `import_delete_originals`/`import_keep_originals` → `import_delete_files`/`import_keep_files`, reading simply **Delete** / **Keep**; `import_deleted` now reports the space recovered. `SafImporter.java`: `Batch` splits `folder` (our destination) from `source` (what the user's folder is called, nullable — the offer never invents a name); `offerDelete` takes `List<Item>` rather than `List<Uri>` so it can sum sizes both for the offer and for the report, and puts **Keep in the positive slot** so the destructive answer is not where a reflex tap lands; `sweepPartials` clears stale `<name>.part` under the destination at the start of every import — the only thing here deleted without asking, because it is ours and belongs to an import that did not finish; the free-space precheck now skips items already present at the same size (summing the whole batch made a re-import of a mostly-imported folder refuse itself); `mb()` → `size()`, GB above 1024 MB. `web/index.html` and `web/setup.html`: "the original files" → "the dictionary files", the chip `original files only` → `dictionary files only`, and the locked-chip tooltip likewise — that phrase named the *imported copy* while the import dialog used "originals" for the user's own files, which is the collision that made the keep/delete question unanswerable.

Not done, deliberately: the requested silent deletion of the "copied sources". The premise did not hold — the copy under `Android/data/<pkg>/files/Dictionaries` is the permanent library, not a temporary, and the files the dialog offers to remove are the user's own on shared storage (`copied` holds the SAF URIs, deleted via `DocumentsContract.deleteDocument`). The share-target route, which is the one that *can* be handed a provider's cache, already offers no deletion (`deletable = false`). Verified: `cd android && ./gradlew assemblePlayDebug` builds clean (one pre-existing deprecation note in `Storage.java`), `go build ./...` green.

### P76 — The keyboard leaves when the reading starts — 2026-08-16 — COMPLETE (uncommitted)
A phone session is: type a word, read the article, scroll — and the IME stayed up through all of it, holding a third of the screen for a control the user had finished with. The only dismissal was the Back key, which is a thing to know rather than a thing to notice. `MainActivity.hideImeOnScroll(web)` takes the scroll gesture as the signal: an `OnTouchListener` on the WebView that never consumes (returns false always), remembers where the gesture began, and on the first `ACTION_MOVE` past `getScaledTouchSlop()` **with vertical dominance** hides the IME once per gesture. Vertical dominance is not decoration — a drag inside the search field to select the word just typed is horizontal, and it is the one gesture where the keyboard must stay. `hideIme` uses `WindowInsetsController.hide(ime())` on API 30+ (guarded by `getRootWindowInsets().isVisible(ime())`, which also skips the lookup entirely for sessions that never opened the keyboard) and `InputMethodManager.hideSoftInputFromWindow` below it.

Two design points. It is in the SHELL, not the page: articles render in sandboxed iframes sized to their content, so a drag over an article is delivered to the child document and no listener in `index.html` can see it, whereas the WebView sees every touch in the window regardless of which document takes it — and an IME is a platform input concern, so the pages stay unaware Android exists (D54). And it hides the IME **without blurring**: the focused element keeps focus, caret, selection and query, so tapping the field brings the keyboard back through the WebView's own path — the same state the Back key already leaves behind, which is why re-showing needs no code. Not covered, and out of scope: a phone browser pointed at a desktop `wudict serve` has no shell, so it keeps its browser's own behaviour. Verified: `./gradlew assemblePlayDebug assembleFossDebug` clean; the shipped `web/` assets are unchanged by this phase.

### P77 — Look up a selection from any app — 2026-08-16 — COMPLETE (uncommitted)
Three ways into a lookup from outside the app, one activity behind all of them (D67). `LookupActivity` is a floating dialog (`Theme.WuWeiDict.Lookup`, day/night, `windowIsFloating` + `windowCloseOnTouchOutside`, sized in `onCreate` to `min(95 % width, 640dp) × 70 % height`) that sits over whatever the user was reading; Back or a tap outside returns them to their selection, and Back is deliberately *not* wired to the WebView's history — a popup is one answer to one selection. The string arrives as `EXTRA_PROCESS_TEXT` → `EXTRA_PROCESS_TEXT_READONLY` → `EXTRA_TEXT` → `?q=` on the URI (with the opaque-URI case handled, since a hand-written `wudict:lookup?q=x` has no `//`), is bounded and collapsed, and becomes `Shell.searchUrl(q, mode, dict)` — the page's own deep link, so no Go and no page change. `taskAffinity=""` + `excludeFromRecents` keep it out of the app's task; a bottom "Open in WuWeiDict" chip hands the same query to `MainActivity` (`EXTRA_QUERY`, singleTask) when a quick answer turns into real reading. It never calls `Storage.ensureAccess`: answering a selection made in another app with a permission dialog would be an ambush.

**Sizing correction, found on device before anything shipped:** the popup came up as a strip containing only the handoff row. A floating window is measured from its CONTENT (the decor measures the content view with `AT_MOST`), so `Window.setLayout` alone is not a size — the content wrapped, and the WebView, being `height=0, weight=1`, was given the excess of a parent that had none. The size is now stated in pixels on the content view (`setContentView(root, new ViewGroup.LayoutParams(w, h))`) with `setLayout` kept so the window agrees with what it holds, computed from `getCurrentWindowMetrics().getBounds()` (the window we are in, not the display — foldables, split-screen) and recomputed in `onConfigurationChanged`, since rotation is in `configChanges` and nothing else would. Worth stating for the same reason it was worth finding: no permission is involved in any of this — a floating ACTIVITY is stacked over the previous app by the window manager, while `SYSTEM_ALERT_WINDOW` is for windows drawn outside an activity, which is the bubble route D67 rejected.

The shell was refactored to support a second window rather than duplicated: `Shell` (origin, `searchUrl`, WebView settings, external-link policy, the `window.open` sink), `Ime` (P76's hide-on-scroll), `Power` (D64's decision, now counting visible windows) and `ServerProcess` (the shared child as a state machine with `ensure`/`retain`/`release`, so the popup works with `MainActivity` dead and closing it never kills the server). `MainActivity` shrank by ~120 lines and behaves identically with no popup open. `tools/verify-apk.sh` now asserts `PROCESS_TEXT` in **both** flavours — the filters live in `src/main`, and nothing fails to build if they rot, the entry simply stops appearing — and tightened the Play permission check from "no storage permission" to "INTERNET and nothing else", which is the assertion this feature has to keep true. Verified: `make check` clean; `make apk`, `make apk-play-debug`, `make apk-foss-release`, `make apk-play-release` all build; `sh tools/verify-apk.sh` green on all four APKs; the merged manifest shows the three filters and the dialog theme on `LookupActivity`. On-device behaviour is the user's to check.

### P78 — The stylesheet is the layout — 2026-08-16 — COMPLETE (uncommitted)
`format=clean` and `format=text` now read the dictionary's own CSS before stripping it (D68). New `internal/htmlref/css.go`: a byte-scanner over the stylesheet — comments removed up front, quote-aware block matching, `@media`/`@supports`/`@layer`/`@container`/`@scope` walked into and `@font-face`/`@keyframes`/`@page` skipped whole — producing `Styles`, a `map[string]Display` over `Unset < None < Inline < Block` merged by max, with `Styles.Class(attr)` an allocation-free index walk over the class attribute. `Sanitize` gained `Policy.Elem` and, more subtly, **one** pending-end stack in place of the old `unwrapped []string`: unwrapped and renamed elements must share an ordering, or `<span>x<span class="Sense">y</span>z</span>` closes its `<div>` in the wrong place. With no `Elem` hook set the emitted bytes are identical to before. `Text` gained the same two rules plus a skip depth, and reads attributes only when a stylesheet exists.

Server side: `internal/server/articlestyle.go` finds the `<link rel=stylesheet href="/res/<id>/…">` of the first result, reads each resource through the backend under a byte budget and folds them into one table cached on the registry `entry` behind `styleMu`; `articleformat.go` turns that into the `Elem` hook, renaming only meaning-free elements (`<span>` and anything not in `cleanKeep`) so an `<a>` styled as a block keeps being an `<a>`. Derivation happens once per response and only when `format != raw`.

Verified: `make check` clean (`go vet` + full suite under `sqlite_fts5`), tag-less `go build ./...` and `-tags purego` vet + tests green. 28-case `TestParseCSS` plus garbage/merge/accumulate tests, and sanitize tests asserting the LDOCE shapes directly — block spans become blocks, `FIELD` disappears with its content, a hidden subtree cannot be re-opened by a block inside it, and a class can never rescue a dropped `<script>`.

### P79 — The extension stops looking like a network prober — 2026-08-17 — COMPLETE (uncommitted)
Hovering a word on `www.google.com` made the browser ask *"www.google.com is asking you to access other apps and services on this device"* — the Local Network Access gate (Chrome/Edge 142+, Firefox 151+), naming the right origin: a subresource a content script puts in the page's DOM is the **page's** request, and a `http://127.0.0.1:6888` image or `<audio>` makes the site the user is reading the one reaching loopback. Nothing in a FAQ beats not triggering it, so the loopback URL was removed from the page entirely and the host permission with it (D69).

Server side (this repo): new `internal/server/cors.go` — `extensionOrigin` accepts only a well-formed `chrome-extension://<id>` / `moz-extension://<uuid>` (scheme, non-empty host, no path, query or userinfo, so `null`, `file://` and every http(s) origin are refused); `allowOrigin` narrows that to `BROWSER_EXTENSIONS` when set; `withCORS` echoes the origin and always sends `Vary: Origin`, never `*` and never `Allow-Credentials`. Wrapped around exactly `GET /api/dicts`, `GET /api/search`, `GET /res/` in `server.go`, with three `OPTIONS` handlers (204, `Allow-Methods: GET`, `Allow-Headers: accept`, `Max-Age: 600`) so a future preflight does not meet `ServeMux`'s 405. Everything destructive or private stays same-origin, and `cors_test.go` asserts that per route rather than trusting the wiring. Config key + env `BROWSER_EXTENSIONS` through the existing ladder; README row; `pages/docs/extension.md` reworded for users.

Extension side (`wudict-browser-extension`, separate repo): `host_permissions` dropped from both manifests and kept as `optional_host_permissions`; Chrome gains `key` (stable id — `BROWSER_EXTENSIONS` can name it) and the `offscreen` permission; `make validate-*` asserts all three so they cannot rot back. New `background/media.js` proxies `/res/` — origin- and prefix-checked, `image/*`/`audio/*` only, 4 MiB cap on both the declared and the real body, base64 in 8 KiB chunks — behind a 64-entry/16 MiB LRU and new `background/inflight.js` (one request per key, dropped on settle, so a rejection is never replayed). `sanitize.js` parks server-origin media in `data-wd-src`; the popup hydrates it through a 200 px-margin IntersectionObserver into blob URLs and revokes them on prune, hide and destroy. Popup audio no longer constructs `Audio` in the page: the worker fetches, and `common/player.js` decodes it in an offscreen document (Chrome, closed after 30 s idle per D64) or the event page (Firefox), which makes stop-on-close mandatory rather than polite. `inflight` also coalesces identical search candidates — the joiner replays the leader's cache entry and loses only progressive rendering, keeping the NDJSON fan-out single-consumer.

Diagnostics had to change with the transport: a CORS refusal and a dead port are the same rejection to `fetch`. `smoke()` now follows a failure with a `mode:'no-cors'` probe and reports `unreachable` vs `no-cors-grant` vs ok; the reason rides on the health record, the toolbar panel names it ("Cannot reach …" vs "… is not answering extensions"), and the **Grant access** button — the host-permission fallback for an older server, which needs a user gesture the worker never has — appears only in the case it can fix. Verified: `make check` clean in both repos, tag-less `go build ./...` and `-tags purego` green here; 30 new extension unit tests over `media.js` and `inflight.js` (origin and `/res/` refusal, MIME refusal, both size caps, chunk-boundary base64 against `Buffer`, dedup, no caching of failures). On-device behaviour — the prompt not appearing on a real page in both browsers — is the user's to check.

### P80 — The mark gets its proportions audited — 2026-08-17 — COMPLETE (uncommitted)

The question was whether the icon follows the golden mean; the measurement said it followed nothing, and the defects it did have were ordinary ones. Ink bbox centre (17.30, 20.00) against (16, 16), margins 5.81 / 3.21 / 9.81 / 1.81, the handle 0.78 px from the tile's corner radius at 16 px, the second text line terminating with zero clearance on the lens ring, and not one stroke edge on a pixel boundary at 16 px. D70 records why φ is the wrong target on an integer grid and what replaces it: Fibonacci 13 : 8 for the text lines, even widths on odd centres so `y/2 ± w/4` is whole, the ink translated to bbox centre (16.00, 16.50), and 2.44 units of clearance between the second line and the ring.

Applied in lockstep to the five surfaces that carry the mark with nothing mechanical linking them — `internal/server/web/favicon.svg` and its two byte-identical copies under `pages/docs/assets/`, `android/app/src/main/res/drawable/ic_launcher_foreground.xml` with its `ic_launcher_background` colour, and `tools/make-icons.mjs` in `wudict-browser-extension` (PNGs regenerated, 8 files). The three geometric rules are written into the SVG and the generator as comments, because the next edit to move a coordinate will look harmless. Tile `#5b7a99` → `#4c6680` (white mark 4.48:1 → 5.97:1, same hue and saturation); the extension's `off` tint `#8a8a8a` → `#7d8085`, the same hue desaturated rather than an unrelated grey, 3.63:1 composited and 1.5× off `on` in luminance. The web UI's `--link` token is still `#5b7a99` at 4.48:1 — that is text, it misses AA for text, and it is left alone here rather than folded into an icon change.

Verified: `make check` green here; `make check` green in the extension (237 tests, both manifest validations); the regenerated 128 px and 16 px PNGs and the SVG rasterisation inspected rather than assumed. Nothing committed.

### P81 — A dictionary that files its words under serial numbers — 2026-08-19 — COMPLETE (uncommitted)

`gramo` in Larousse's *Gran Diccionario* rendered under the heading **E310420**. Not a parse failure: that dictionary genuinely keys all 86,916 entries by internal identifier and carries the word in the definition's title field. Checking the store made the real damage visible — `contains` and `full-text` index the headword column only, so the entire dictionary was substring-searchable by identifier and not at all by word, and 674,585 alternates carried words that no `contains` query could reach.

Measured before writing anything, across the 18-file BGL corpus, because three plausible fixes all had to be rejected on evidence: promote the title always (destroys Babylon Spanish-English and Merriam-Webster Collegiate), promote per entry on the alternate test (rewrites `A`→`Å` in MW Collegiate and `about time`→`time` in Larousse Compact), or patch the display layer (leaves the indexes broken). What survives is a whole-file verdict sampled in the metadata pass: 99.0 % of the id-keyed file's sample carries the signature against ≤1.1 % everywhere else. D71 has the rule and the numbers. The identifier stays as an alternate — a `bword://D53650` grep over real articles showed the dictionary cross-links by id, so dropping it would have broken every internal link.

Entry framing and entry decoding are now separate (`splitEntry`/`decodeEntry`), which is what lets the metadata pass sample entries before the encodings are known, and `processDefi` split into `collectDefiFields` + `renderDefi` so an entry that wants its title too scans the field set once. Verified: `make check` green, tag-less build and `-tags purego` vet clean, three new table cases over the detector (id-keyed, ordinary, below the 20-entry floor), and the real file re-read end to end — `gramo` now arrives as `[gramo E310420 gramito gramitos gramos]` while all fourteen normally-keyed corpus dictionaries report `idKeyed=false` with their headwords unchanged. **The user must re-prepare that dictionary** (delete `~/.wudict/db/Gran_Diccionario_de_la_Lengua_/`): a prepared library folder is not re-ingested by a reader change. Nothing committed.

### P82 — The article that kept growing — 2026-08-19 — COMPLETE (uncommitted)
LDoCE's *writer* (`en-en-LDOCE2023-online-audio.slob`) showed a long blank strip under the entry. Not a content bug — the body the API returns ends exactly at `</div>` — and not D60's ratchet either, which was fixed and stayed fixed. `ldoce.css` line 12 says `html, body { height: 100% }`, so inside a content-sized iframe body's box, the root's box and the root's scrolling area all became functions of the height we had just reported; measured in Chrome the frame grew 22px per `ResizeObserver` pass toward the parent's 60000px cap.

Fixed where it starts: `frameDoc` now appends `:root,:root>body{height:auto!important;min-height:0!important;max-height:none!important}` **after** the article's markup (after, because a dictionary's own `<link>` sits inside the body; `:root>body` rather than `html body`, so an `!important` in the article still loses). D60's four measurements are untouched, and `post()` gains only a backstop for an article that rewrites its own head: when body's box equals the viewport to the pixel, it measures the bottom of body's in-flow element children instead.

A first attempt measured a `Range` over body's contents and was withdrawn after browser testing: a Range unions out-of-flow descendants, so an absolutely positioned box near the viewport bottom restarts the runaway — Merriam-Webster Online 2438 → 2679px — and trailing margins vanish. Verification was a headless-Chrome harness over 25 script-bearing dictionaries from the user's library plus 14 synthetic pathologies: identical to today on 24 of 25, LDoCE alone changing (1702 → 1571), shrink-in-place still settling 822 → 322. `make check` green; the page's `{{FRAMEJS}}` tag is a content hash, so browsers pick the new script up without a cache clear — but the binary must be rebuilt, both files are embedded. Decision recorded as D72.

Flagged, not fixed: `OED(20200912)` (mdx) and `OED-2020` (slob) hit the parent's 60000px clamp on *water* — their ink measures 177,533px and 235,500px — and `html{overflow-y:hidden}` makes the remainder unreachable. Needs its own decision (raise the cap, or let an over-cap frame scroll itself).

### P83 — The definition that vanished under the reader — 2026-08-21 — COMPLETE (uncommitted)

Typing a word, pausing, and starting to read produced a definition that was replaced under the eye as slower-but-higher-preference dictionaries reported. Diagnosis before design: the slots were **already** extrinsically ordered — the server sends the dictionary list on `begin`, boxes are created empty and in order, and `msg.i` addresses them — so no result has ever changed another result's position. The bug was one clause in `doSearch` that closed the open section when a lower-index dictionary arrived, plus `out.innerHTML=""` on `begin` blanking the whole area once per keystroke-pause.

Shipped in `internal/server/web/index.html` only — no server change, no new files, no dependency, no animation, no tuning constant:

- **`promote(boxes,i)`** (new, above `renderSlot`) — when a slot takes the latch, every slot above it that is still empty moves below it. Empty boxes have no height, so the reorder is invisible; afterwards nothing can arrive above the section being read. No scroll position is ever touched.
- **`openedIdx` moved to module scope**, so a click on a summary can set it to `-1` permanently; the auto-open guard lost its `msg.i<openedIdx` promotion branch.
- **Append-then-drop** — the new generation's boxes are appended *below* the previous answer on `begin` (they are empty divs: nothing moves, nothing shows) and the old nodes are removed on the first rendering hit, or on `end`. Not a detached `DocumentFragment` swap, which was the first design and is wrong here: an inline `<style>` in a shadow root has a null `.sheet` until connected, so rendering an article off-document would make `hoistShadowFonts` silently drop every inline `@font-face`. The widening re-run deliberately leaves the previous answer on screen.
- **`revealAt` and the summary-click correction** are unchanged from before P83 — with no anchor logic to fight, the `smoothUntil` marking they briefly carried is gone.

**Redesigned mid-phase, after user testing.** The first implementation cancelled the shift with a compensating `scrollBy` (`keepPlace`) and carved out `scrollY === 0`. On a word with many hits (`pot`) the sections still piled up above the open one and pushed it down — the carve-out excluded exactly the case that matters, because a freshly opened definition is read at the top of the page. The user also rejected programmatic scrolling on principle. `promote` replaces it: `keepPlace`, `smoothUntil`, both carve-outs, the smooth-scroll marking and the Safari `overflow-anchor` reasoning are all deleted, for a net reduction in code. D73's M4 section records both designs and why the second is not merely simpler but correct where the first was not.

D73 records the four state machines, the rejected grace window, and why `overflow-anchor` could not be the mechanism (Safari).

**Flagged, not fixed.** The `phase === "partial"` re-run at the end of `doSearch` — a search issued while the dictionary list is still streaming re-runs itself when the rest lands. That is a *new generation*: new slot set, latch re-armed, reading position lost, possibly mid-article. M2 keeps it from blanking but does not prevent it. Fixing it properly means suppressing the re-run once the user has scrolled or interacted, which is a behaviour change to D30 and belongs in its own phase.

**Validated by a 12-test Playwright suite** (scratchpad, not landed:
`…/scratchpad/web-tests/`). It drives the `/api/search` NDJSON stream line by
line through a `fetch` shim backed by a `ReadableStream`, so arrival order is
exact and no test sleeps. Discrimination was proven by stashing `index.html`,
rebuilding and re-running against the pre-fix binary: 8 of the 13 fail there,
at least one per defect. The M4 tests assert the section top *and* `window.scrollY`
together — the first was achievable with a compensating scroll, and the pair is
what pins the promotion design. Two of them boot with `overflow-anchor:none`
injected so that Chromium's native scroll anchoring cannot be what is holding
the position.

### P84 — Somewhere to see it, and somewhere to stop it — 2026-08-22 — COMPLETE (uncommitted)

A non-technical user starting WuWeiDict had no way to tell the server was running and no way to stop it short of `kill`. This adds an optional tray icon that is **off on every existing launch path** — terminal, launchd agent, systemd unit, Android — and on only when the binary can tell it was launched from a desktop, or when asked with `--tray`/`TRAY=1`.

Built defensively because the library requires it. Read from `github.com/gogpu/systray@v0.2.8`'s source rather than its README: `New()`, `Show()` and `SetMenu()` all discard their platform errors, `Run()` is the only call that returns one and it blocks, Linux `Run()` is a bare `<-t.quit`, and failing to register with the StatusNotifierWatcher is a `slog.Warn` after which `Create()` returns nil. So on GNOME without the AppIndicator extension every call reports success and the process hangs forever on an invisible icon. Our own preflight is the only thing standing between that and a user. The spike also answered the macOS main-thread question by reading `internal/init.go` — the library already calls `runtime.LockOSThread()` unconditionally, so no `darwin` init of ours is needed; the side effect is that importing it pins the main goroutine for *every* wudict command.

Shipped:

- **`internal/tray`** (new, 7 files) — machine T as an explicit transition table with `Degraded`/`Gone` absorbing; `Wrap(cfg, serve)` starting `serve()` before anything platform-facing and returning its error unchanged; `recover()` around both `Start` and `Run`; a cancellable `os.Exit` watchdog armed only against a hung message loop. The platform sits behind a 3-method interface, so **17 tests drive the whole machine with no desktop, no D-Bus and no window server** — preflight failure, `Start` error, `Start` panic, `Run` returning early, Quit fired twice, the server ending on its own, menu shape, and every legal and illegal transition. `go test -race -count=2` clean.
- **Preflight per platform** — Linux asks D-Bus `NameHasOwner("org.kde.StatusNotifierWatcher")`, bounded at 3 s with a buffered result channel so a timeout cannot leak a blocked goroutine; macOS refuses outside an `.app` bundle and under `SSH_CONNECTION`; Windows passes unconditionally.
- **`internal/logx`** — `SetOutput(io.Writer)`/`Output()`, mutex-guarded, backing all six former `os.Stderr` sites. Redirecting makes `Interactive()` report false, so `Progress`/`ClearLine` self-silence into a log file exactly as they already did into a pipe.
- **`internal/cli/cli.go`** — `--tray`/`--no-tray`; the shutdown path refactored to one `sync.Once`-guarded `stop()` reachable from both a signal and the tray; `httpSrv.Serve` extracted into a `serve` closure; `printStartup` and three raw `Fprintf(os.Stderr, …)` sites moved onto `logx`; `openHeadlessLog()`; and the seam itself, `return tray.Wrap(…, serve)`, which is a pass-through whenever the tray is off.
- **`internal/config`** — `TRAY` through the existing precedence ladder as a `*bool` (tri-state: unset means "decide from the launch"), plus its line in the generated `wudict.toml`.
- **`internal/server`** — `Reveal(path)` exported so the tray's "Open dictionary folder" drives the same action as `/api/reveal` without going through the mux.
- **`tools/make-icons.sh` + `internal/tray/icons/`** — two committed PNGs rendered from `favicon.svg`. They live under `internal/tray/` because `go:embed` cannot reach outside its package directory; the plan said `packaging/icons/` and that path could not have worked.
- **Makefile** — `make icons`, ~~`make build-windows-gui`, and `cross` now emitting `wudictw-windows-{amd64,arm64}.exe` alongside the console binaries~~ (**removed 2026-08-22 by D76/P86**: one `.exe`).

Verified: `make check` green; tag-less `go build ./...`; `go vet -tags purego ./...`; builds clean for darwin, linux, windows, freebsd and android; and the Android assertion — `GOOS=android go list -deps ./internal/cli` shows **no** `gogpu`, `godbus` or `goffi`, which is what `//go:build !android` buys without a new build tag. `make build-windows-gui` produces a real 13 MB `wudictw.exe` (that target existed only between P84 and P86). Nothing committed.

**Not verified here, by design:** no icon has been drawn on a screen. The library's macOS path sets `NSApplicationActivationPolicyAccessory`, so `--tray` from a terminal should produce a menu-bar item before P85 exists — that is the user's next hands-on check, along with `wudict serve` in a terminal being byte-identical to before, and `DBUS_SESSION_BUS_ADDRESS= wudict serve --tray` on Linux printing one line and serving normally.

**Deferred, sketched in D74 and the plan file:** P85 (macOS `.app` bundle — `packaging/darwin/Info.plist.in` + `tools/make-app.sh` + `make mac-app`, `LSUIElement=1`, and the `com.legbehindneck.wudict` bundle-id question) and P86 (Inno per-user installer, "Start at login", `.mdx/.dsl/.slob/.bgl` associations — which need an "open this file" entry point the binary does not have yet — and a native notification for the degraded-GUI-launch case, which today has no liveness indicator at all).

### P85 — A thing you can double-click — 2026-08-22 — COMPLETE (uncommitted)

P84 built a tray icon nobody could reach: on macOS it only appears when the binary can tell a person launched it, and the only signal for that is an `.app` bundle path. This is the bundle. It changes no product behaviour — a bare `wudict` already serves, a second launch already opens the browser at the first and exits — it only produces the layout and the plist that make macOS treat the binary as an app.

Worth recording because it inverts the obvious assumption: **the bundle is not what makes the tray possible.** The library's darwin path is pure runtime AppKit — `[NSStatusBar systemStatusBar]`, `setActivationPolicy:Accessory` — with no `NSBundle` or `Info.plist` reference anywhere, so `wudict serve --tray` draws a menu-bar item on an unbundled binary today. The bundle is what makes it *automatic*.

Shipped:

- **`packaging/darwin/Info.plist.in`** — the template, in the same `<!-- TEMPLATE -->`-stripped style as `launchctl/*.plist.in`. `LSUIElement` (menu bar, no Dock), `LSMinimumSystemVersion` 12.0 (Go 1.26's own floor), `NSSupportsSuddenTermination` false (open SQLite handles), and a comment on every key that is not self-evident.
- **`tools/make-app.sh`** — builds `dist/<name>.app`, `cp`ing the binary (never symlinking: `GUILaunched()` calls `EvalSymlinks` and a link would resolve out of the bundle), rendering the plist, `plutil -lint`ing it, failing on any unsubstituted `@TOKEN@`, and ad-hoc signing. **Name and version are read from `wudict --version`**, so the bundle cannot drift from `cli.ProductName`; the derived path is written to `dist/.app-path` because Make cannot know a name it did not choose.
- **`make mac-app` / `make mac-app-install`** — the second installs into `~/Applications`: per-user, no sudo, matching the per-user preference already set for the Windows installer. `APP_BIN`, `APP_ID`, `APP_DEST`, `MACOS_MIN`, `CODESIGN_ID` override.
- **`tools/make-icons.sh` + `packaging/darwin/wudict.icns`** — ten sizes through `iconutil`, rendered from the one SVG (D70), committed. Skips with a note where `iconutil` does not exist.
- **`.github/workflows/build-cgo.yml`** — a `macos-app` job that `lipo`s the two darwin artifacts into one **universal** bundle and `ditto`s it to `wudict-macos-app.zip`; `release` now waits on it, so the zip lands on the release page with the binaries.
- **Docs** — `pages/docs/install.md` (the no-terminal route), `pages/docs/service.md` (app vs LaunchAgent, and what happens when both want the port), `README.md`.

Verified on this machine: `make mac-app` produces `dist/wuDict.app`; `plutil -p` shows all 17 keys substituted with `CFBundleIdentifier = com.legbehindneck.wudict`, `CFBundleShortVersionString = 2.9.0` from `v2.9-dirty`, `LSUIElement = true`; `codesign --verify --strict` reports *valid on disk* and *satisfies its Designated Requirement*; the executable is a real Mach-O at `Contents/MacOS/wudict`; the workflow parses.

**Not verified here, by design:** nothing has been opened. `open dist/wuDict.app` → a menu-bar icon, no Dock icon, a browser tab, and `~/Library/Logs/wudict.log` filling with the banner is the user's check.

**Still deferred:** P86 (Windows Inno per-user installer, "Start at login", `.mdx/.dsl/.slob/.bgl` associations — which need an "open this file" entry point the binary still does not have — and a native notification for the degraded-GUI-launch case).

### P86 — One `.exe`, and an installer that does not ask for your password — 2026-08-22 — COMPLETE (uncommitted)

Two things at once, because the second was blocked on the first. P84 had accepted a second Windows binary — `wudictw.exe`, linked `-H windowsgui` — on the grounds that "no console window" is a linker flag. **It is; but keeping the window is not.** A console-subsystem `wudict.exe` can ask who else is attached to its console and give the window back when the answer is "nobody", which is exactly the double-click case. So there is now **one `.exe`**, and the installer has one thing to install, one thing to associate and one thing to put on `PATH`. See **D76**.

Shipped — the one-`.exe` half:

- **`internal/tray/platform_windows.go`** — rewritten around `GetConsoleProcessList` (count `1` ⇒ Windows made this console for us alone ⇒ GUI launch), `FreeConsole`, `ProcessIdToSessionId` (session 0 has no interactive desktop; preflight used to pass unconditionally) and `MessageBoxW`. All via `syscall.NewLazyDLL` — `x/sys/windows` exports none of the first three — so no new dependency.
- **`internal/cli/cli.go`** — `GUILaunched()` read **once** and carried as `tray.Config.GUI`, because `FreeConsole` changes its answer; the headless log opened **before** the console is released, because a black window beats total silence; detaching confined to the serve path.
- **`internal/tray/tray.go`** — `degrade()` now raises a native modal when `cfg.GUI`, on a detached goroutine. Under D74 this was a nice-to-have; without a console it is the only remaining indicator. `notify` is implemented on Windows (`MessageBoxW`) and macOS (`osascript display alert`, with AppleScript string escaping), and is a no-op elsewhere.
- **`internal/server/hidewindow_{windows,other}.go`** — `speexdec` is spawned with `CREATE_NO_WINDOW`. A console-less parent starting a console child gets a **new** console window for it, so without this a detached server flashes a black box on every `.spx` decode.
- **Makefile** — `GUI_LDFLAGS`, `build-windows-gui` and the `cross` `wudictw` branch deleted.

Shipped — the installer:

- **`packaging/windows/wudict.iss`** — Inno Setup 6, `PrivilegesRequired=lowest` (no UAC, installs into `%LOCALAPPDATA%\Programs`), fixed `AppId` GUID, `CloseApplications=force` so an upgrade can replace a running server, and four unchecked-or-checked optional tasks: Desktop shortcut, **Start at sign-in** (a `{userstartup}` shortcut with `--no-browser`, not a `Run` key), user-`PATH` entry (added by `[Registry]`, removed surgically in `[Code]` — `uninsdeletevalue` on `Path` would delete the whole variable), and file associations.
- **Associations** for `.mdx/.dsl/.slob/.bgl` as a ProgId joined to each extension's `OpenWithProgids`. Not the default handler: since Windows 10 only the user can set that, and writing the key anyway gets it reverted with a warning notice.
- **`wudict <dictfile>`** — the entry point the associations point at, and the product change P84 and P85 both listed as missing. Serves the **folder** holding the file (companions only pair up through a folder scan), tested last in `Main`'s `default:` case behind a format-registry name check *and* a regular-file `stat`, so a typo is still an unknown command. Table-driven test in `internal/cli/openfile_test.go` covers `.mdd` (readable, not a dictionary), a directory named `dir.mdx`, a missing file and a bare word.
- **`tools/version.sh`** — extracted from `make-app.sh`; both packagers now read the name and version from `wudict --version`, so they exist once, in `internal/cli`.
- **`tools/make-installer.sh` + `make win-installer`** — finds `iscc` on `PATH` or in either standard install location, and says how to get it where it is absent.
- **`packaging/windows/wudict.ico`** — six sizes from the same SVG (D70), packed by `tools/make-icons.sh` with twelve lines of `python3`, since a post-Vista `.ico` entry may be a PNG verbatim. Committed.
- **`.github/workflows/build-cgo.yml`** — a `windows-installer` job downstream of `build`, which `choco install`s Inno if the runner image ever stops shipping it; `release` waits on it too.
- **Docs** — `pages/docs/install.md`, `pages/docs/service.md`, `README.md`.

Verified on this machine: `go build ./...` clean for darwin, windows and linux; `go vet ./internal/cli/ ./internal/dict/` clean; `gofmt` clean; the new `TestOpenFileArgs` passes and `wudict nonsense` still exits 2 with *unknown command*; `make icons` regenerates a structurally valid 6-entry `.ico` (verified by unpacking the directory and checking every offset lands on a PNG signature inside the file); the workflow parses and `release.needs` is `[build, macos-app, windows-installer]`; `make check` green.

**Not verified here, by design:** no Windows machine was involved. The installer has not been compiled (`iscc` is Windows-only — CI does it), no console has been released, and no `MessageBoxW` has been seen. The user's checks are: a double-clicked `wudict.exe` shows a tray icon and **no** black window; `wudict lookup x | more` from `cmd.exe` still pipes and still returns to the prompt afterwards; the installer runs without a UAC prompt; and a `.mdx` offers wuDict under "Open with".
